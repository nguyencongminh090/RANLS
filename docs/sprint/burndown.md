# Burndown — current sprint

One row per day the sprint is touched (not necessarily every calendar day). `Remaining` = sum of
points on `docs/sprint/current.md` items not yet `✅`. `Ideal` = linear reference line from total
points at sprint start to 0 at the end date.

| Date | Remaining | Ideal | Note |
|---|---|---|---|
| 2026-08-21 | 6 / 6 items | — | Sprint 4 opened (UI-03, UX-01, UX-02, UX-03, UX-04, CLEAN-01); Sprint 3's final row is archived in `docs/sprint/archive/sprint-3.md` |
| 2026-08-21 | 7 / 7 items | — | UX-05 pulled into Active same day (surfaced by UX-04's own investigation) |
| 2026-08-21 | 0 / 7 items | — | All seven items landed on `main` this same day — burndown row wasn't added at the time; backfilled 2026-08-30 during Sprint 4 close-out |
| 2026-08-30 | — | — | Sprint 4 formally closed and archived to `docs/sprint/archive/sprint-4.md`; table reset below for Sprint 5 |
| 2026-08-30 | 4 / 4 items | — | Sprint 5 opened (IO-01, DOC-01, TOOL-01, CLEAN-02) — see `docs/sprint/current.md` |
| 2026-08-30 | 0 / 4 items | — | Sprint 5 closed same day — all four items landed on `main`; archived to `docs/sprint/archive/sprint-5.md`; table reset below for Sprint 6 |
| 2026-08-30 | 4 / 4 items | — | Sprint 6 opened (UI-04, UI-05, UX-06, UI-06) — UI-review items filed the same day; see `docs/sprint/current.md` |
| 2026-08-30 | 3 / 4 items | — | UI-04 landed on `main` — `GomocupProtocol::clearAnalysisState()` wired to position-change + `signal_analysis` gated on `isAnalyzing()`; regression test `tests/test_ui04_pv_reset.cpp` added |
| 2026-08-30 | 3 / 4 items | — | UI-06 implementation complete (new `MatchConfig` + "Engine plays" menu + `generateMoveRequest`/`requestEngineMove` auto-move path); builds clean, unit tests pass. Not counted as landed — pending a live-engine smoke pass |
| 2026-08-30 | 3 / 4 items | — | UX-06 implementation complete (removed `uiProfile`; WinGraph SingleSide/BothSide reworked + relabelled + reads `MatchConfig::enginePlays`; `applyAppTheme` helper; Settings dialog rebuilt as resizable tabbed Notebook; +2 settings round-trip tests). Builds clean, 119/119 tests pass. Not counted as landed — pending a human interactive smoke pass |
| 2026-08-30 | 3 / 4 items | — | UX-06 second pass after a real GUI run: coordinate labels were dark-on-dark outside the wood (now use themed `get_color()`); `prefer_dark_theme` is a no-op on KDE (now also force `gtk-theme-name` Adwaita/Adwaita-dark); WinGraph modes only differ with eval data (pure logic extracted to `win_graph_series.h` + `test_ux06_wingraph_series.cpp`). Theme/coordinates/dialog visually verified via screenshots; 123/123 tests pass. Still not landed — populated-WinGraph check needs a human |
| 2026-08-30 | 3 / 5 items | — | UI-07 added to Active (user's UX-06/UI-06 smoke surfaced still-broken PV accumulation with the real compact `MESSAGE depth` format). Implementation complete: `AnalysisPanel::signal_board_changed` now refreshes `pvView_`/`engineStatus_` from `gameState_` — protocol layer verified clean by 2 new real-format regression tests (`test_ui07_pv_cross_position.cpp`). Build clean, 125/125 tests pass. Not landed — post-position-change screenshot needs a human/interactive check |
| 2026-08-30 | 2 / 5 items | — | UI-07 **landed** on the branch (second pass). The first pass's `AnalysisPanel` refresh was necessary but insufficient; the real cause was `PVView::update()` calling `Gtk::ListBox::remove()` with the row's inner `Gtk::Box` (a grandchild), which GTK 4 ignores — one orphan row widget leaked per PV clear. Now wraps/removes an explicit `Gtk::ListBoxRow`. New gtkmm-linking test target `rapfi-gui-ui-tests` (4 cases, asserts rendered widget tree) closes the blind spot that let the first pass ship green; reproduced and verified against the real `pbrain-rapfi`. 125/125 + 4/4 pass, build clean |
| 2026-08-30 | 3 / 6 items | — | STATE-04 added to Active — UI-06's smoke pass surfaced that rule + board size are never written to the settings file (pre-existing, not a UI-06 regression; `engine_plays` itself round-trips fine). Filed + designed same day (rule → global preference, board size → new-game default) and dispatched via `/implement-task` |
| 2026-08-30 | 2 / 6 items | — | STATE-04 implemented and verified: new `GameSetupConfig` (rule + boardSize) threaded through `SettingsBundle` + a 4th `save()` param; `load()`/`save()` round-trip `rule` + `board_size` with validate-or-fallback; `MainWindow` restores both at startup, persists via `persistGameSetup()` from `onSetRule()` + Board Size Apply, `onNewGame()` keeps current size; all 3 `save()` call sites pass the full four-block state. +4 round-trip tests. Build clean, 129/129 + UI tests pass; live app launch consumes a hand-written `rule`/`board_size` file (menu click-through not scriptable here) |
| 2026-08-30 | 1 / 6 items | — | UI-06 **closed** by the user after a live-engine smoke pass ("Engine plays" menu + auto-move verified working; the same pass is what surfaced STATE-04). Also reconciled UI-05's `current.md` row (was still "Active" though its detail file + `TODO.md` had it ✅). Only UX-06 remains open in Sprint 6 (impl complete — needs a human populated-WinGraph check). Sprint work committed |
| 2026-08-31 | 0 / 6 items | — | UX-06 **verified** by the user (populated-WinGraph Single↔Two-line check) → ✅ VERIFIED. Sprint 6 closed and archived to `docs/sprint/archive/sprint-6.md`; nothing rolled over. Table reset below for Sprint 7 |
| 2026-08-31 | 5 / 5 items | — | Sprint 7 opened (goal "UI polish + release prep"): UI-08, ENG-02, UI-09, REL-01, REL-02 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-08-31 | 3 / 5 items | — | ENG-02 landed. Manual intervention (toolbar/hotkey Stop, analysis-panel Stop, Analyze on the engine's own turn, dispatcher `analyze`/`!play` on the engine's turn) now reverts `MatchConfig::enginePlays` to `Off` in memory only — no `SettingsStorage::save`, so the persisted side is restored next launch. Shared pure predicate `isEnginesTurn()` extracted to `model/config.h` (+`test_eng02_revert_predicate.cpp`, 3 cases). Build clean; `rapfi-gui-tests` + `rapfi-gui-ui-tests` pass. (UI-08 also already ✅ in `TODO.md` — remaining: UI-09, REL-01, REL-02) |
| 2026-08-31 | 2 / 5 items | — | UI-09 landed. `WinGraphMode::SingleSide` is now unconditionally Black's perspective (UX-06's `enginePlays` coupling removed — rationale in the fix-log detail; `buildWinGraphSeries` param kept but discarded, pinned by a rewritten regression case). Win-rate line thickened 1.5→2.8 px (White 1.2→2.6) and recoloured blue `#1A73E8` + green `#1E8E3E` — clears WCAG 3:1 non-text contrast on both the light `#fafafb` and dark `#242424` Adwaita panel backgrounds (old yellow White line was 1.59:1 on light); `dataviz` validator all-PASS both modes; White line keeps its dash for CVD shape redundancy. `config.h`/`win_graph_series.h` docs + Settings label updated. Build clean; `rapfi-gui-tests` 132/132 + `rapfi-gui-ui-tests` 6/6 pass. Remaining: REL-01, REL-02 |
| 2026-08-31 | 1 / 5 items | — | REL-01 landed (doc/process only). Root `CHANGELOG.md` created ("Keep a Changelog" 1.1.0, SemVer 0.x): `[Unreleased]` + a single `## [0.1.0] - 2026-08-31` backfilled from `docs/sprint/archive/sprint-1..6.md` + `docs/fix-log.md` as user-impact lines. "Cutting a release" checklist added to the `github` skill + one-line `CLAUDE.md` "Sprint cadence" pointer; `docs/audit/2026-08-31-changelog-and-release-process.md` records the decision. Tag `v0.1.0` created on `main` and pushed. No source / no `CMakeLists.txt` touched; both tracking-file check scripts pass. Remaining: REL-02 |

| 2026-08-31 | 0 / 5 items | — | REL-02 landed. Single source of truth is now `project(rapfi-gui VERSION 0.1.0)` in the top-level `CMakeLists.txt`; `configure_file` generates `build/generated/version.h` (`#define APP_VERSION`) from `src/version.h.in`. `src/main.cpp` scans argv for `--version`/`-v` at the very top of `main()` (before `RapfiApplication::create()` / any GTK init) and prints `APP_VERSION`; `onAbout()`'s `set_version("2.0")` → `set_version(APP_VERSION)`. Script-based ctest `rel02-version-single-source` runs the real binary headless and asserts `--version`/`-v` output == CMake `PROJECT_VERSION` (links no gtkmm). `game_io.cpp` `kFormatVersion`/`yxgame_version` left untouched (save-file schema). Build clean; ctest 3/3 green (`rapfi-gui-tests`, `rel02-version-single-source`, `rapfi-gui-ui-tests`); `env -u DISPLAY -u WAYLAND_DISPLAY ./build/rapfi-gui --version` → `0.1.0` rc=0, no window. All 5 Sprint 7 items now landed. |

| 2026-08-31 | — | — | Sprint 7 closed same day — all five items landed on `main`; archived to `docs/sprint/archive/sprint-7.md`; nothing rolled over. Table reset below for Sprint 8 |
| 2026-08-31 | 2 / 2 items | — | Sprint 8 opened (goal "Engine-log sticky-bottom + About-window rewrite"): UI-10, UI-11 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-08-31 | 1 / 2 items | — | UI-10 landed on branch `ui-10/engine-log-not-sticky-to-bottom-during-analysis`. Root cause: `scrollToEnd()`'s create-`scroll_to`-`delete_mark` triplet destroyed GTK4's deferred-scroll target before the post-relayout pass ran (primary), and `isScrolledToBottom()` trusting a mid-stream stale `upper` latched `wasAtBottom` false so auto-scroll disabled itself (compounding). Fix: one persistent `engineLogEndMark_` re-scrolled once on `Glib::signal_idle`, plus a remembered `stickToBottom_` intent flipped only in the vadjustment settle handler. Pure decision logic → `src/ui/sticky_scroll.h` + `test_ui10_sticky_scroll.cpp` (5 cases). Build clean, ctest 3/3. Manual live-engine streaming check NOT run (no engine binary on this machine) — noted in the fix-log detail. Remaining: UI-11 |
| 2026-08-31 | 1 / 2 items | — | UI-10 merged to `main` via PR #2 (squash `c9248e6`); branch + worktree cleaned up. Orchestrator re-verified independently before merge: clean Debug build, `ctest` 3/3 (137 assertions incl. 5 new UI-10 cases). `current.md` status → Done. Manual live-engine streaming check still outstanding (deferred — no engine binary available). Remaining: UI-11 |
| 2026-08-31 | 1 / 2 items | — | UI-10 **reopened + refixed** — user retested PR #2 and the Engine Log still showed the first line during analysis. Real root cause: the Engine Log TextView was inside `EmptyStateOverlay` (a `Gtk::Overlay`, not `Gtk::Scrollable`) so GTK interposed a `GtkViewport` and every `TextView::scroll_to` — including PR #2's — was a silent no-op. PR #4 (`5b0bb13`): `set_child(engineLogView_)` directly + a `programmaticScroll_` guard on the `value_changed` stick-tracking. Regression tests upgraded to real-widget behavioural cases (`test_ui10_engine_log_scroll_target.cpp`, 3 cases, all fail pre-fix). Clean build, ctest 3/3. Move Log has the same latent no-op → filed UI-12 (Backlog). Lesson: PR #2 shipped without live/real-widget verification and its unit tests only covered pure helpers — a wrong GTK assumption slipped straight through. Remaining: UI-11 |
| 2026-08-31 | 2 / 3 items | — | UI-12 pulled from Backlog into Sprint 8 Active (same `Gtk::Overlay`-breaks-`Gtk::Scrollable` no-op as UI-10, in the Move Log). Sprint 8 now UI-10 ✅ / UI-11 / UI-12; Backlog empty |
| 2026-08-31 | 1 / 3 items | — | UI-12 **landed** on `main` via PR #5 (squash `a47e343`). `scrolledMoveLog_.set_child(moveLogView_)` directly + non-static `scrollMoveLogToEnd()` (persistent right-gravity `moveLogEndMark_`, re-issued once on idle); removed the dead `moveLogOverlay_`/`engineLogOverlay_` members + `updateMoveLogEmptyState()`/`updateEngineLogEmptyState()` + `empty_state.h` include. New `test_ui12_move_log_scroll_target.cpp` (2 real-widget cases, both fail pre-fix). "Don't yank a scrolled-up user" deliberately out of scope (burst-append log). Orchestrator re-verified: clean Debug build, no new warnings, ctest 3/3 (`rapfi-gui-ui-tests` 11 cases / 70 assertions). Branch + worktree cleaned. Remaining: UI-11 |
| 2026-09-01 | 0 / 3 items | — | UI-11 **merged** to `main` via PR #6 (squash `b9cacfd`); branch + worktree cleaned. Orchestrator re-verified independently: clean `rm -rf build` Debug build (no new warnings), `ctest` 3/3 (`rapfi-gui-ui-tests` 13 cases / 81 assertions incl. new `test_ui11_about_dialog.cpp`), `rel02-version-single-source` green. `current.md` UI-11 → Done. All 3 Sprint 8 items now landed — sprint ready to close + cut `v0.2.0`. Manual light/dark visual check of the running About dialog still outstanding (no display/engine on the build host). |
| 2026-08-31 | 0 / 3 items | — | UI-11 implemented on branch `ui-11/about-window-rewrite` (pending merge). Stock `Gtk::AboutDialog` → new custom `AboutDialog` class (`src/ui/about_dialog.{h,cpp}`): two-pane layout (themed stock icon + info column), `RANLS` heading, `APP_VERSION`, developer credit, tech/build-info grid (GTK/gtkmm/Cairo runtime versions, build date, git commit, license), clickable repo + Gomocup/Yixin protocol links, supported-engine list; single Close button, Esc closes. New `src/build_info.h.in` (build date via `string(TIMESTAMP … UTC)`, git short hash via guarded configure-time `git rev-parse`, `"unknown"` fallback). `APP_VERSION` still single-sourced (REL-02) — `rel02-version-single-source` green. App-wide `Rapfi Analysis → RANLS` rename split out as NAME-01 (not done here); no binary logo asset committed. +`test_ui11_about_dialog.cpp` (3 real-widget cases incl. Adwaita/Adwaita-dark build). Clean build, ctest 3/3 (`rapfi-gui-ui-tests` 13 / 81). Manual light/dark visual check of the running app outstanding for the orchestrator. Sprint 8 now UI-10 ✅ / UI-11 ✅ / UI-12 ✅ |

| 2026-09-03 | — | — | Sprint 8 closed — all three items (UI-10, UI-11, UI-12) landed on `main`; archived to `docs/sprint/archive/sprint-8.md`, nothing rolled over. Release `v0.1.1` cut (fix/polish bundle: Engine + Move Log auto-scroll, About window rewrite) — `CHANGELOG.md` `[Unreleased]` → `[0.1.1] - 2026-09-03`, CMake `project(VERSION)` 0.1.0 → 0.1.1, tag `v0.1.1` pushed. Table reset below for Sprint 9 |
| 2026-09-03 | 2 / 2 items | — | Sprint 9 opened (goal "WinGraph coverage + app-wide RANLS rename"): UI-13, NAME-01 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-09-03 | 1 / 2 items | — | UI-13 implemented on branch `ui-13/wingraph-record-eval-regardless-of-side` (pending merge). Scope narrowed with the user to candidate **A** + the `flush()`-before-move ordering fix. `GameState::setAnalysisData` now also fills an already-existing played-line child node for `bestPv.moves[0]` with the complementary win% (`1 - score`) at a derived `depth > 0` (never fabricates a node; never overwrites real analysis; same "only if changed" + `treeDirty_` path). `EngineController::signal_move` reordered `setAnalyzing(false)` → `flush()` → `signal_engine_move` → `setState(Idle)` so the searched position's final analysis lands before the board advances and before any state-change handler runs (RT-01 unchanged). +`tests/test_ui13_wingraph_eval_coverage.cpp` (4 model-layer cases). Build clean (no new warnings); ctest 3/3 (model 141 / 1112, UI 14). Candidates B/C and "evaluate the whole line" deferred to Backlog. Remaining: NAME-01 |
| 2026-09-03 | 1 / 2 items | — | UI-13 **merged** to `main` via PR #7 (squash `6133e41`); branch + worktree cleaned. Orchestrator re-verified independently: clean `rm -rf build_cmd` build (no new warnings — only the 3 pre-existing `-Wunused-function` in `gomocup_protocol.cpp`), `ctest` 3/3 (model `rapfi-gui-tests` 141 cases / 1112 assertions incl. new `test_ui13_wingraph_eval_coverage.cpp`; UI `rapfi-gui-ui-tests` 14; `rel02-version-single-source` green). `current.md` UI-13 → Done. Remaining: NAME-01 (Sprint 9's last Active item). |

| 2026-09-03 | 0 / 2 items | — | NAME-01 **merged** to `main` via PR #8 (squash `0ec6cd2`); branch pushed + PR opened + squash-merged by the user (auto-mode classifier blocked the orchestrator's `gh pr merge`). Orchestrator re-verified independently on the branch before merge: `rm -rf build` clean Debug configure + build (exit 0; only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `./build/ranls-gui --version` → `0.1.1`, `ctest` 3/3 (model `ranls-gui-tests` 141 cases / 1112 assertions; UI `ranls-gui-ui-tests` 16 cases / 88 assertions incl. new `test_name01_window_title.cpp` +2; `rel02-version-single-source` green). Full scope per user decision: window title (`kAppDisplayName` constant), `style.css` comment, GTK app id `com.rapfi.analysis` → `com.ranls.gui`, CMake `DESCRIPTION` + build targets `rapfi-gui{,-tests,-ui-tests}` → `ranls-gui*` (+ `build.sh`, `build_msys2.sh`, `README.md`, `check_version.cmake`). `rapfi-gui.settings` filename left as-is (exe-relative, not app-id-derived). `current.md` NAME-01 → Done. **All Sprint 9 Active items (UI-13, NAME-01) now landed — sprint ready to close + cut a release.** |
| 2026-09-03 | — | — | Sprint 9's last item landed. Next: `github` skill "Cutting a release" checklist (not run unprompted). |
| 2026-09-04 | 0 / 2 items | — | Sprint 9 closed — UI-13, NAME-01 landed on `main` (PRs #7, #8); archived to `docs/sprint/archive/sprint-9.md`, nothing rolled over (the deferred "evaluate the whole line" work was filed fresh as ANLZ-01/02/03, not a rollover). Release `v0.1.2` cut (PATCH — WinGraph per-ply coverage fix + completion of the RANLS rename). `docs/sprint/current.md` reset for Sprint 10. |
| 2026-09-04 | 1 / 1 items | — | Sprint 10 opened (goal "Analyze Mode — continuous background analysis for full WinGraph coverage"): ANLZ-01 pulled from Backlog into Active — see `docs/sprint/current.md`. `features/analyze-mode/planning.md` Q1–Q8 resolved with the user the same day (all 8 proposed defaults accepted verbatim), which unblocked the pull. Backlog now has 2 items (ANLZ-02, ANLZ-03 — both `_detail TBD_`, follow ANLZ-01). |
| 2026-09-04 | 1 / 1 items | — | ANLZ-01 implemented on branch `anlz-01/continuous-analyze-mode` (pending merge). `ViewConfig::analyzeMode` + `analyze_mode=` persistence; `MainWindow::scheduleAnalyzeModeRestart()` + `analyzeModeScheduled_` (verbatim `maybeStartAutoMove()` idle-coalescing) wired to `signal_board_changed` + engine→Idle; guards `analyzeMode && isRunning() && engineState()==Idle`, bail on `isEnginesTurn`, else `stopAnalysis(); analyze()` (both reused unchanged). Checkable `analyze-mode` menu action + EngineStatus "∞" toggle + `syncAnalyzeModeMenu()` (mirrors `syncEnginePlaysMenu()`). Orthogonal to ENG-02; UI-13 candidate A + test intact; one-shot Analyze/Stop additive; Q5 estimate deferred. +`test_anlz01_analyze_mode_coverage.cpp` (model, 3 cases) +`test_anlz01_analyze_mode_action.cpp` (real `MainWindow`) +1 settings round-trip case. Build clean (3 pre-existing warnings only); ctest 3/3. Not counted landed — manual live-engine smoke needs a human (no engine binary / display on the build host). |

| 2026-09-04 | 0 / 1 items | — | ANLZ-01 **merged** to `main` via PR #9 (squash `0ae2b8a`); branch + worktree cleaned; user allowed the merge (auto-mode classifier blocked the orchestrator's `gh pr merge`). Orchestrator re-verified independently on the agent's worktree before merge: `./build.sh` clean (only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `ctest` 3/3 (`ranls-gui-tests` 145 cases / 1150 assertions incl. new `test_anlz01_analyze_mode_coverage.cpp` +3 + settings round-trip +1; `ranls-gui-ui-tests` 17 cases / 94 assertions incl. new `test_anlz01_analyze_mode_action.cpp`; `rel02-version-single-source` green). `current.md` ANLZ-01 → Done. **Sprint 10's sole Active item now landed — sprint ready to close + cut a release.** Manual live-engine smoke still needs a human (no engine binary / display on the build host — checklist in `docs/fix-log/2026-09-04-analyze-mode.md`). |

| 2026-09-04 | 1 / 2 items | — | **ANLZ-04 pulled from Backlog into Sprint 10 Active** (mid-sprint, after ANLZ-01 already shipped). WinGraph rendering-side complement to ANLZ-01: faint dashed bridge across residual NaN runs instead of disjoint segments (always on, no gap cap; still no dot / "(no eval)" for gap plies). Pure `WinGraphView::onDraw` change, needs a `docs/audit/` entry for the UI-01 refinement. Milestone `Sprint 10` + labels `sprint:10` / `area:ANLZ` assigned. Backlog now has 1 item (ANLZ-03). Next: `/implement-task ANLZ-04`. |
| 2026-09-04 | 0 / 2 items | — | ANLZ-04 implemented on branch `anlz-04/wingraph-bridge-nan-gaps` (pending merge). New pure helper `computeGapBridges()` (`src/ui/win_graph_bridge.h`) → one `{fromIdx,toIdx}` pair per interior NaN run (none for leading/trailing / `n<=1` / all-NaN); `WinGraphView::onDraw` gained a pass-1 loop per series stroking those connectors dashed `{4,3}` (distinct from the UI-09 White dash `{6,4}`), series colour @ 0.4 alpha, width `kSeries*W*0.6`, one accumulated path + one `stroke()` + `unset_dash()` — then the pre-existing solid-run loop (pass 2) unchanged. Current-move-dot guard + hover `(no eval)` untouched; nothing written back to data, no synthesised 50%. `buildWinGraphSeries` / `evalHistory` sentinel / eval maths / UI-09 / RT-01 / axes untouched; no flag/setting/gap-cap. +`tests/test_anlz04_wingraph_bridge.cpp` (8 cases / 21 assertions, `ranls-gui-tests`). `./build.sh` clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. `docs/audit/2026-09-04-wingraph-nan-bridge.md` + row (UI-01 refinement) + `docs/fix-log/2026-09-04-anlz-04-wingraph-nan-bridge.md` + row. Visual/live smoke not possible (no display) — substituted by the helper unit test. Not counted landed — pending merge. |

To render this as a chart, ask for it explicitly (Artifact, on request — see `/CLAUDE.md` "Sprint
cadence": don't auto-render on every update).
