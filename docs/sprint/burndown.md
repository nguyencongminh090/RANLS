# Burndown — current sprint

One row per day the sprint is touched (not necessarily every calendar day). `Remaining` = sum of
points on `docs/sprint/current.md` items not yet `✅`. `Ideal` = linear reference line from total
points at sprint start to 0 at the end date.

| Date | Remaining | Ideal | Note |
|---|---|---|---|
| 2026-08-21 | 6 / 6 items | — | Sprint 4 opened (UI-03, UX-01, UX-02, UX-03, UX-04, CLEAN-01); Sprint 3's final row is archived in `docs/sprint/archive/sprint-3.md` |
| 2026-08-21 | 7 / 7 items | — | UX-05 pulled into Active same day (surfaced by UX-04's own investigation) |
| 2026-08-21 | 0 / 7 items | — | All seven items landed on `main` this same day — burndown row wasn't added at the time; backfilled 2026-08-30 during Sprint 4 close-out |
| 2026-08-30 | — | — | Sprint 4 formally closed and archived to `docs/sprint/archive/sprint-4.md`; table reset below for Sprint 5 |
| 2026-08-30 | 4 / 4 items | — | Sprint 5 opened (IO-01, DOC-01, TOOL-01, CLEAN-02) — see `docs/sprint/current.md` |
| 2026-08-30 | 0 / 4 items | — | Sprint 5 closed same day — all four items landed on `main`; archived to `docs/sprint/archive/sprint-5.md`; table reset below for Sprint 6 |
| 2026-08-30 | 4 / 4 items | — | Sprint 6 opened (UI-04, UI-05, UX-06, UI-06) — UI-review items filed the same day; see `docs/sprint/current.md` |
| 2026-08-30 | 3 / 4 items | — | UI-04 landed on `main` — `GomocupProtocol::clearAnalysisState()` wired to position-change + `signal_analysis` gated on `isAnalyzing()`; regression test `tests/test_ui04_pv_reset.cpp` added |
| 2026-08-30 | 3 / 4 items | — | UI-06 implementation complete (new `MatchConfig` + "Engine plays" menu + `generateMoveRequest`/`requestEngineMove` auto-move path); builds clean, unit tests pass. Not counted as landed — pending a live-engine smoke pass |
| 2026-08-30 | 3 / 4 items | — | UX-06 implementation complete (removed `uiProfile`; WinGraph SingleSide/BothSide reworked + relabelled + reads `MatchConfig::enginePlays`; `applyAppTheme` helper; Settings dialog rebuilt as resizable tabbed Notebook; +2 settings round-trip tests). Builds clean, 119/119 tests pass. Not counted as landed — pending a human interactive smoke pass |
| 2026-08-30 | 3 / 4 items | — | UX-06 second pass after a real GUI run: coordinate labels were dark-on-dark outside the wood (now use themed `get_color()`); `prefer_dark_theme` is a no-op on KDE (now also force `gtk-theme-name` Adwaita/Adwaita-dark); WinGraph modes only differ with eval data (pure logic extracted to `win_graph_series.h` + `test_ux06_wingraph_series.cpp`). Theme/coordinates/dialog visually verified via screenshots; 123/123 tests pass. Still not landed — populated-WinGraph check needs a human |
| 2026-08-30 | 3 / 5 items | — | UI-07 added to Active (user's UX-06/UI-06 smoke surfaced still-broken PV accumulation with the real compact `MESSAGE depth` format). Implementation complete: `AnalysisPanel::signal_board_changed` now refreshes `pvView_`/`engineStatus_` from `gameState_` — protocol layer verified clean by 2 new real-format regression tests (`test_ui07_pv_cross_position.cpp`). Build clean, 125/125 tests pass. Not landed — post-position-change screenshot needs a human/interactive check |
| 2026-08-30 | 2 / 5 items | — | UI-07 **landed** on the branch (second pass). The first pass's `AnalysisPanel` refresh was necessary but insufficient; the real cause was `PVView::update()` calling `Gtk::ListBox::remove()` with the row's inner `Gtk::Box` (a grandchild), which GTK 4 ignores — one orphan row widget leaked per PV clear. Now wraps/removes an explicit `Gtk::ListBoxRow`. New gtkmm-linking test target `rapfi-gui-ui-tests` (4 cases, asserts rendered widget tree) closes the blind spot that let the first pass ship green; reproduced and verified against the real `pbrain-rapfi`. 125/125 + 4/4 pass, build clean |
| 2026-08-30 | 3 / 6 items | — | STATE-04 added to Active — UI-06's smoke pass surfaced that rule + board size are never written to the settings file (pre-existing, not a UI-06 regression; `engine_plays` itself round-trips fine). Filed + designed same day (rule → global preference, board size → new-game default) and dispatched via `/implement-task` |
| 2026-08-30 | 2 / 6 items | — | STATE-04 implemented and verified: new `GameSetupConfig` (rule + boardSize) threaded through `SettingsBundle` + a 4th `save()` param; `load()`/`save()` round-trip `rule` + `board_size` with validate-or-fallback; `MainWindow` restores both at startup, persists via `persistGameSetup()` from `onSetRule()` + Board Size Apply, `onNewGame()` keeps current size; all 3 `save()` call sites pass the full four-block state. +4 round-trip tests. Build clean, 129/129 + UI tests pass; live app launch consumes a hand-written `rule`/`board_size` file (menu click-through not scriptable here) |
| 2026-08-30 | 1 / 6 items | — | UI-06 **closed** by the user after a live-engine smoke pass ("Engine plays" menu + auto-move verified working; the same pass is what surfaced STATE-04). Also reconciled UI-05's `current.md` row (was still "Active" though its detail file + `TODO.md` had it ✅). Only UX-06 remains open in Sprint 6 (impl complete — needs a human populated-WinGraph check). Sprint work committed |
| 2026-08-31 | 0 / 6 items | — | UX-06 **verified** by the user (populated-WinGraph Single↔Two-line check) → ✅ VERIFIED. Sprint 6 closed and archived to `docs/sprint/archive/sprint-6.md`; nothing rolled over. Table reset below for Sprint 7 |
| 2026-08-31 | 5 / 5 items | — | Sprint 7 opened (goal "UI polish + release prep"): UI-08, ENG-02, UI-09, REL-01, REL-02 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-08-31 | 3 / 5 items | — | ENG-02 landed. Manual intervention (toolbar/hotkey Stop, analysis-panel Stop, Analyze on the engine's own turn, dispatcher `analyze`/`!play` on the engine's turn) now reverts `MatchConfig::enginePlays` to `Off` in memory only — no `SettingsStorage::save`, so the persisted side is restored next launch. Shared pure predicate `isEnginesTurn()` extracted to `model/config.h` (+`test_eng02_revert_predicate.cpp`, 3 cases). Build clean; `rapfi-gui-tests` + `rapfi-gui-ui-tests` pass. (UI-08 also already ✅ in `TODO.md` — remaining: UI-09, REL-01, REL-02) |
| 2026-08-31 | 2 / 5 items | — | UI-09 landed. `WinGraphMode::SingleSide` is now unconditionally Black's perspective (UX-06's `enginePlays` coupling removed — rationale in the fix-log detail; `buildWinGraphSeries` param kept but discarded, pinned by a rewritten regression case). Win-rate line thickened 1.5→2.8 px (White 1.2→2.6) and recoloured blue `#1A73E8` + green `#1E8E3E` — clears WCAG 3:1 non-text contrast on both the light `#fafafb` and dark `#242424` Adwaita panel backgrounds (old yellow White line was 1.59:1 on light); `dataviz` validator all-PASS both modes; White line keeps its dash for CVD shape redundancy. `config.h`/`win_graph_series.h` docs + Settings label updated. Build clean; `rapfi-gui-tests` 132/132 + `rapfi-gui-ui-tests` 6/6 pass. Remaining: REL-01, REL-02 |
| 2026-08-31 | 1 / 5 items | — | REL-01 landed (doc/process only). Root `CHANGELOG.md` created ("Keep a Changelog" 1.1.0, SemVer 0.x): `[Unreleased]` + a single `## [0.1.0] - 2026-08-31` backfilled from `docs/sprint/archive/sprint-1..6.md` + `docs/fix-log.md` as user-impact lines. "Cutting a release" checklist added to the `github` skill + one-line `CLAUDE.md` "Sprint cadence" pointer; `docs/audit/2026-08-31-changelog-and-release-process.md` records the decision. Tag `v0.1.0` created on `main` and pushed. No source / no `CMakeLists.txt` touched; both tracking-file check scripts pass. Remaining: REL-02 |

| 2026-08-31 | 0 / 5 items | — | REL-02 landed. Single source of truth is now `project(rapfi-gui VERSION 0.1.0)` in the top-level `CMakeLists.txt`; `configure_file` generates `build/generated/version.h` (`#define APP_VERSION`) from `src/version.h.in`. `src/main.cpp` scans argv for `--version`/`-v` at the very top of `main()` (before `RapfiApplication::create()` / any GTK init) and prints `APP_VERSION`; `onAbout()`'s `set_version("2.0")` → `set_version(APP_VERSION)`. Script-based ctest `rel02-version-single-source` runs the real binary headless and asserts `--version`/`-v` output == CMake `PROJECT_VERSION` (links no gtkmm). `game_io.cpp` `kFormatVersion`/`yxgame_version` left untouched (save-file schema). Build clean; ctest 3/3 green (`rapfi-gui-tests`, `rel02-version-single-source`, `rapfi-gui-ui-tests`); `env -u DISPLAY -u WAYLAND_DISPLAY ./build/rapfi-gui --version` → `0.1.0` rc=0, no window. All 5 Sprint 7 items now landed. |

| 2026-08-31 | — | — | Sprint 7 closed same day — all five items landed on `main`; archived to `docs/sprint/archive/sprint-7.md`; nothing rolled over. Table reset below for Sprint 8 |
| 2026-08-31 | 2 / 2 items | — | Sprint 8 opened (goal "Engine-log sticky-bottom + About-window rewrite"): UI-10, UI-11 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-08-31 | 1 / 2 items | — | UI-10 landed on branch `ui-10/engine-log-not-sticky-to-bottom-during-analysis`. Root cause: `scrollToEnd()`'s create-`scroll_to`-`delete_mark` triplet destroyed GTK4's deferred-scroll target before the post-relayout pass ran (primary), and `isScrolledToBottom()` trusting a mid-stream stale `upper` latched `wasAtBottom` false so auto-scroll disabled itself (compounding). Fix: one persistent `engineLogEndMark_` re-scrolled once on `Glib::signal_idle`, plus a remembered `stickToBottom_` intent flipped only in the vadjustment settle handler. Pure decision logic → `src/ui/sticky_scroll.h` + `test_ui10_sticky_scroll.cpp` (5 cases). Build clean, ctest 3/3. Manual live-engine streaming check NOT run (no engine binary on this machine) — noted in the fix-log detail. Remaining: UI-11 |
| 2026-08-31 | 1 / 2 items | — | UI-10 merged to `main` via PR #2 (squash `c9248e6`); branch + worktree cleaned up. Orchestrator re-verified independently before merge: clean Debug build, `ctest` 3/3 (137 assertions incl. 5 new UI-10 cases). `current.md` status → Done. Manual live-engine streaming check still outstanding (deferred — no engine binary available). Remaining: UI-11 |
| 2026-08-31 | 1 / 2 items | — | UI-10 **reopened + refixed** — user retested PR #2 and the Engine Log still showed the first line during analysis. Real root cause: the Engine Log TextView was inside `EmptyStateOverlay` (a `Gtk::Overlay`, not `Gtk::Scrollable`) so GTK interposed a `GtkViewport` and every `TextView::scroll_to` — including PR #2's — was a silent no-op. PR #4 (`5b0bb13`): `set_child(engineLogView_)` directly + a `programmaticScroll_` guard on the `value_changed` stick-tracking. Regression tests upgraded to real-widget behavioural cases (`test_ui10_engine_log_scroll_target.cpp`, 3 cases, all fail pre-fix). Clean build, ctest 3/3. Move Log has the same latent no-op → filed UI-12 (Backlog). Lesson: PR #2 shipped without live/real-widget verification and its unit tests only covered pure helpers — a wrong GTK assumption slipped straight through. Remaining: UI-11 |
| 2026-08-31 | 2 / 3 items | — | UI-12 pulled from Backlog into Sprint 8 Active (same `Gtk::Overlay`-breaks-`Gtk::Scrollable` no-op as UI-10, in the Move Log). Sprint 8 now UI-10 ✅ / UI-11 / UI-12; Backlog empty |
| 2026-08-31 | 1 / 3 items | — | UI-12 **landed** on `main` via PR #5 (squash `a47e343`). `scrolledMoveLog_.set_child(moveLogView_)` directly + non-static `scrollMoveLogToEnd()` (persistent right-gravity `moveLogEndMark_`, re-issued once on idle); removed the dead `moveLogOverlay_`/`engineLogOverlay_` members + `updateMoveLogEmptyState()`/`updateEngineLogEmptyState()` + `empty_state.h` include. New `test_ui12_move_log_scroll_target.cpp` (2 real-widget cases, both fail pre-fix). "Don't yank a scrolled-up user" deliberately out of scope (burst-append log). Orchestrator re-verified: clean Debug build, no new warnings, ctest 3/3 (`rapfi-gui-ui-tests` 11 cases / 70 assertions). Branch + worktree cleaned. Remaining: UI-11 |
| 2026-09-01 | 0 / 3 items | — | UI-11 **merged** to `main` via PR #6 (squash `b9cacfd`); branch + worktree cleaned. Orchestrator re-verified independently: clean `rm -rf build` Debug build (no new warnings), `ctest` 3/3 (`rapfi-gui-ui-tests` 13 cases / 81 assertions incl. new `test_ui11_about_dialog.cpp`), `rel02-version-single-source` green. `current.md` UI-11 → Done. All 3 Sprint 8 items now landed — sprint ready to close + cut `v0.2.0`. Manual light/dark visual check of the running About dialog still outstanding (no display/engine on the build host). |
| 2026-08-31 | 0 / 3 items | — | UI-11 implemented on branch `ui-11/about-window-rewrite` (pending merge). Stock `Gtk::AboutDialog` → new custom `AboutDialog` class (`src/ui/about_dialog.{h,cpp}`): two-pane layout (themed stock icon + info column), `RANLS` heading, `APP_VERSION`, developer credit, tech/build-info grid (GTK/gtkmm/Cairo runtime versions, build date, git commit, license), clickable repo + Gomocup/Yixin protocol links, supported-engine list; single Close button, Esc closes. New `src/build_info.h.in` (build date via `string(TIMESTAMP … UTC)`, git short hash via guarded configure-time `git rev-parse`, `"unknown"` fallback). `APP_VERSION` still single-sourced (REL-02) — `rel02-version-single-source` green. App-wide `Rapfi Analysis → RANLS` rename split out as NAME-01 (not done here); no binary logo asset committed. +`test_ui11_about_dialog.cpp` (3 real-widget cases incl. Adwaita/Adwaita-dark build). Clean build, ctest 3/3 (`rapfi-gui-ui-tests` 13 / 81). Manual light/dark visual check of the running app outstanding for the orchestrator. Sprint 8 now UI-10 ✅ / UI-11 ✅ / UI-12 ✅ |

| 2026-09-03 | — | — | Sprint 8 closed — all three items (UI-10, UI-11, UI-12) landed on `main`; archived to `docs/sprint/archive/sprint-8.md`, nothing rolled over. Release `v0.1.1` cut (fix/polish bundle: Engine + Move Log auto-scroll, About window rewrite) — `CHANGELOG.md` `[Unreleased]` → `[0.1.1] - 2026-09-03`, CMake `project(VERSION)` 0.1.0 → 0.1.1, tag `v0.1.1` pushed. Table reset below for Sprint 9 |
| 2026-09-03 | 2 / 2 items | — | Sprint 9 opened (goal "WinGraph coverage + app-wide RANLS rename"): UI-13, NAME-01 pulled from Backlog into Active — see `docs/sprint/current.md`. Backlog now empty |
| 2026-09-03 | 1 / 2 items | — | UI-13 implemented on branch `ui-13/wingraph-record-eval-regardless-of-side` (pending merge). Scope narrowed with the user to candidate **A** + the `flush()`-before-move ordering fix. `GameState::setAnalysisData` now also fills an already-existing played-line child node for `bestPv.moves[0]` with the complementary win% (`1 - score`) at a derived `depth > 0` (never fabricates a node; never overwrites real analysis; same "only if changed" + `treeDirty_` path). `EngineController::signal_move` reordered `setAnalyzing(false)` → `flush()` → `signal_engine_move` → `setState(Idle)` so the searched position's final analysis lands before the board advances and before any state-change handler runs (RT-01 unchanged). +`tests/test_ui13_wingraph_eval_coverage.cpp` (4 model-layer cases). Build clean (no new warnings); ctest 3/3 (model 141 / 1112, UI 14). Candidates B/C and "evaluate the whole line" deferred to Backlog. Remaining: NAME-01 |
| 2026-09-03 | 1 / 2 items | — | UI-13 **merged** to `main` via PR #7 (squash `6133e41`); branch + worktree cleaned. Orchestrator re-verified independently: clean `rm -rf build_cmd` build (no new warnings — only the 3 pre-existing `-Wunused-function` in `gomocup_protocol.cpp`), `ctest` 3/3 (model `rapfi-gui-tests` 141 cases / 1112 assertions incl. new `test_ui13_wingraph_eval_coverage.cpp`; UI `rapfi-gui-ui-tests` 14; `rel02-version-single-source` green). `current.md` UI-13 → Done. Remaining: NAME-01 (Sprint 9's last Active item). |

| 2026-09-03 | 0 / 2 items | — | NAME-01 **merged** to `main` via PR #8 (squash `0ec6cd2`); branch pushed + PR opened + squash-merged by the user (auto-mode classifier blocked the orchestrator's `gh pr merge`). Orchestrator re-verified independently on the branch before merge: `rm -rf build` clean Debug configure + build (exit 0; only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `./build/ranls-gui --version` → `0.1.1`, `ctest` 3/3 (model `ranls-gui-tests` 141 cases / 1112 assertions; UI `ranls-gui-ui-tests` 16 cases / 88 assertions incl. new `test_name01_window_title.cpp` +2; `rel02-version-single-source` green). Full scope per user decision: window title (`kAppDisplayName` constant), `style.css` comment, GTK app id `com.rapfi.analysis` → `com.ranls.gui`, CMake `DESCRIPTION` + build targets `rapfi-gui{,-tests,-ui-tests}` → `ranls-gui*` (+ `build.sh`, `build_msys2.sh`, `README.md`, `check_version.cmake`). `rapfi-gui.settings` filename left as-is (exe-relative, not app-id-derived). `current.md` NAME-01 → Done. **All Sprint 9 Active items (UI-13, NAME-01) now landed — sprint ready to close + cut a release.** |
| 2026-09-03 | — | — | Sprint 9's last item landed. Next: `github` skill "Cutting a release" checklist (not run unprompted). |
| 2026-09-04 | 0 / 2 items | — | Sprint 9 closed — UI-13, NAME-01 landed on `main` (PRs #7, #8); archived to `docs/sprint/archive/sprint-9.md`, nothing rolled over (the deferred "evaluate the whole line" work was filed fresh as ANLZ-01/02/03, not a rollover). Release `v0.1.2` cut (PATCH — WinGraph per-ply coverage fix + completion of the RANLS rename). `docs/sprint/current.md` reset for Sprint 10. |
| 2026-09-04 | 1 / 1 items | — | Sprint 10 opened (goal "Analyze Mode — continuous background analysis for full WinGraph coverage"): ANLZ-01 pulled from Backlog into Active — see `docs/sprint/current.md`. `features/analyze-mode/planning.md` Q1–Q8 resolved with the user the same day (all 8 proposed defaults accepted verbatim), which unblocked the pull. Backlog now has 2 items (ANLZ-02, ANLZ-03 — both `_detail TBD_`, follow ANLZ-01). |
| 2026-09-04 | 1 / 1 items | — | ANLZ-01 implemented on branch `anlz-01/continuous-analyze-mode` (pending merge). `ViewConfig::analyzeMode` + `analyze_mode=` persistence; `MainWindow::scheduleAnalyzeModeRestart()` + `analyzeModeScheduled_` (verbatim `maybeStartAutoMove()` idle-coalescing) wired to `signal_board_changed` + engine→Idle; guards `analyzeMode && isRunning() && engineState()==Idle`, bail on `isEnginesTurn`, else `stopAnalysis(); analyze()` (both reused unchanged). Checkable `analyze-mode` menu action + EngineStatus "∞" toggle + `syncAnalyzeModeMenu()` (mirrors `syncEnginePlaysMenu()`). Orthogonal to ENG-02; UI-13 candidate A + test intact; one-shot Analyze/Stop additive; Q5 estimate deferred. +`test_anlz01_analyze_mode_coverage.cpp` (model, 3 cases) +`test_anlz01_analyze_mode_action.cpp` (real `MainWindow`) +1 settings round-trip case. Build clean (3 pre-existing warnings only); ctest 3/3. Not counted landed — manual live-engine smoke needs a human (no engine binary / display on the build host). |

| 2026-09-04 | 0 / 1 items | — | ANLZ-01 **merged** to `main` via PR #9 (squash `0ae2b8a`); branch + worktree cleaned; user allowed the merge (auto-mode classifier blocked the orchestrator's `gh pr merge`). Orchestrator re-verified independently on the agent's worktree before merge: `./build.sh` clean (only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `ctest` 3/3 (`ranls-gui-tests` 145 cases / 1150 assertions incl. new `test_anlz01_analyze_mode_coverage.cpp` +3 + settings round-trip +1; `ranls-gui-ui-tests` 17 cases / 94 assertions incl. new `test_anlz01_analyze_mode_action.cpp`; `rel02-version-single-source` green). `current.md` ANLZ-01 → Done. **Sprint 10's sole Active item now landed — sprint ready to close + cut a release.** Manual live-engine smoke still needs a human (no engine binary / display on the build host — checklist in `docs/fix-log/2026-09-04-analyze-mode.md`). |

| 2026-09-04 | 1 / 2 items | — | **ANLZ-04 pulled from Backlog into Sprint 10 Active** (mid-sprint, after ANLZ-01 already shipped). WinGraph rendering-side complement to ANLZ-01: faint dashed bridge across residual NaN runs instead of disjoint segments (always on, no gap cap; still no dot / "(no eval)" for gap plies). Pure `WinGraphView::onDraw` change, needs a `docs/audit/` entry for the UI-01 refinement. Milestone `Sprint 10` + labels `sprint:10` / `area:ANLZ` assigned. Backlog now has 1 item (ANLZ-03). Next: `/implement-task ANLZ-04`. |
| 2026-09-04 | 0 / 2 items | — | ANLZ-04 implemented on branch `anlz-04/wingraph-bridge-nan-gaps` (pending merge). New pure helper `computeGapBridges()` (`src/ui/win_graph_bridge.h`) → one `{fromIdx,toIdx}` pair per interior NaN run (none for leading/trailing / `n<=1` / all-NaN); `WinGraphView::onDraw` gained a pass-1 loop per series stroking those connectors dashed `{4,3}` (distinct from the UI-09 White dash `{6,4}`), series colour @ 0.4 alpha, width `kSeries*W*0.6`, one accumulated path + one `stroke()` + `unset_dash()` — then the pre-existing solid-run loop (pass 2) unchanged. Current-move-dot guard + hover `(no eval)` untouched; nothing written back to data, no synthesised 50%. `buildWinGraphSeries` / `evalHistory` sentinel / eval maths / UI-09 / RT-01 / axes untouched; no flag/setting/gap-cap. +`tests/test_anlz04_wingraph_bridge.cpp` (8 cases / 21 assertions, `ranls-gui-tests`). `./build.sh` clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. `docs/audit/2026-09-04-wingraph-nan-bridge.md` + row (UI-01 refinement) + `docs/fix-log/2026-09-04-anlz-04-wingraph-nan-bridge.md` + row. Visual/live smoke not possible (no display) — substituted by the helper unit test. Not counted landed — pending merge. |

| 2026-09-04 | 0 / 2 items | — | ANLZ-04 **merged** to `main` via PR #10 (squash `9fb7317`); branch + worktree cleaned. Orchestrator re-verified independently on the agent's branch before merge: `./build.sh` clean (only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `ctest` 3/3 (`ranls-gui-tests` incl. new `test_anlz04_wingraph_bridge.cpp` 8 cases / 21 assertions; `ranls-gui-ui-tests`; `rel02-version-single-source` green). `current.md` ANLZ-04 → Done. **Both Sprint 10 Active items now landed — sprint ready to close + cut a release.** Visual/live WinGraph smoke still needs a human (no display on the build host). |

| 2026-09-04 | 0 / 2 items | — | Sprint 10 **closed** — ANLZ-01, ANLZ-04 landed on `main` (PRs #9, #10); archived to `docs/sprint/archive/sprint-10.md`, nothing rolled over (ANLZ-03's detail + instruction files scaffolded ahead of a Sprint 11 pull; ANLZ-02 dropped earlier). Release `v0.2.0` cut (MINOR — Analyze Mode is a new user-visible feature): `CHANGELOG.md` `[Unreleased]` → `[0.2.0] - 2026-09-04`, CMake `project(VERSION)` 0.1.2 → 0.2.0, tag `v0.2.0` pushed. Milestone `Sprint 10` closed. Table reset below for Sprint 11 |
| 2026-09-04 | 1 / 1 items | — | Sprint 11 **opened** (goal "Persist per-node win% in the save-game file so a reloaded game keeps its WinGraph"): ANLZ-03 pulled from Backlog into Active — see `docs/sprint/current.md`. Milestone `Sprint 11` + labels `sprint:11` / `area:ANLZ`. Backlog now has 1 item (TOOL-02). Next: `/implement-task ANLZ-03`. |
| 2026-09-04 | 3 / 3 items | — | Sprint 11 **re-planned** during `/implement-task ANLZ-03` discussion. User rejected extending the `.yxgame` text schema; chose a new **binary `.rdb`** format (CBOR + DEFLATE, whole variation tree + per-node analysis, tree-not-DAG, single-game). Designed in `features/rdb-save-format/` (Q1–Q8 resolved). **ANLZ-03 → ⛔ SUPERSEDED**; re-split into **RDB-01** (container + `ICompressor` + `GameGraph` DTO + CBOR + convert), **RDB-02** (Save/Open wiring, `GameIO::saveGame` retired, `.yxgame` import-only), **RDB-03** (per-node analysis persistence end-to-end — closes ANLZ-03's goal). `docs/todo/RDB-0{1,2,3}-*.md` + `docs/instruction/RDB-0{1,2,3}-*.md` filed; `TODO.md` / `instruction.md` / `current.md` updated. Integration branch `feat/rdb-save-format`. Next: `/implement-task RDB-01`. |
| 2026-09-04 | 2 / 3 items | — | **RDB-01 merged** to the integration branch `feat/rdb-save-format` via PR #11 (squash `ad69e55`) — not yet on `main` (lands with the final `feat/rdb-save-format → main` PR). Orchestrator re-verified independently on the branch before merge: clean from-scratch `cmake` build (only the 3 pre-existing `-Wunused-function` warnings in `gomocup_protocol.cpp`), `ctest` 3/3 (`ranls-gui-tests` 168 cases / 2155 assertions incl. new `test_rdb01_container.cpp` + `test_rdb01_game_graph.cpp`; `rel02-version-single-source`; `ranls-gui-ui-tests` 17 / 94), `check-tracking-sync.js` exit 0, diff contained to `src/model/rdb/` + tests + build wiring + tracking. `current.md` RDB-01 → merged-to-feature-branch. Next: `/implement-task RDB-02`. Minor note carried to RDB-02: `applyGameGraph` does not yet range-check move coords against board size — RDB-02 replays onto the board so it must validate there. |

| 2026-09-04 | 1 / 3 items | — | **RDB-02 merged** to `feat/rdb-save-format` via PR #12 (squash `d1182d2`) — not yet on `main`. Orchestrator re-verified independently on the branch: clean from-scratch build (3 known warnings only), `ctest` 3/3 (`ranls-gui-tests` 173 cases / 2215 assertions incl. new `test_rdb02_archive.cpp` + `test_rdb02_save_open.cpp`; `rel02-version-single-source`; `ranls-gui-ui-tests`), `check-tracking-sync.js` exit 0, `grep` confirms `GameIO::saveGame` gone. New `src/model/rdb/game_archive.{h,cpp}` (`IGameArchive*` + `RdbArchive` + `YxgameReader` + factory + `applyGameGraphToState` — validates every parent ref + every move coord vs board size before mutating); `onSaveGame`/`onLoadGame` rewired; `.yxgame` write retired, import kept; `docs/audit/2026-09-04-rdb-save-format.md`. `current.md` RDB-02 → merged-to-feature-branch. Next: `/implement-task RDB-03` (last of the three — closes the ANLZ-03 goal). |

| 2026-09-04 | 0 / 3 items | — | **RDB-03 merged** to `feat/rdb-save-format` via PR #13 (squash `51ff892`) — closes the original ANLZ-03 goal (saved `.rdb` reopens with its WinGraph intact, no re-analysis). D1: `evalHistory()` gate → `!isnan(eval)` + `TreeNode::eval` re-defaulted `0.0`→NaN (only consequential fallout: `tree_explorer.cpp` eval-column NaN guard). D2: `std::optional<NodeAnalysisExtras>` on `TreeNode`. `kSchemaVersion` stays 1. Orchestrator re-verified independently: clean from-scratch build (3 known warnings), `ctest` 3/3, `ranls-gui-tests -tc=UI-01*,UI-13*,RDB-03*` → 18 cases / 157 assertions pass (incl. fresh-game all-NaN), tracking-sync exit 0. **All 3 Sprint 11 items now on `feat/rdb-save-format`.** Next: `feat → main` merge PR (`--merge`), then sprint close + `v0.3.0`. |

| 2026-09-04 | 0 / 3 items | — | Sprint 11 **closed** — RDB-01, RDB-02, RDB-03 landed on `main` (sub-PRs #11/#12/#13 into `feat/rdb-save-format`, then merge PR #14 `f44478b` preserving the per-`CODE` commits `1c6b53c`/`df9b4ef`/`03a6718`); archived to `docs/sprint/archive/sprint-11.md`. Nothing rolled over (ANLZ-03 was superseded, not rolled). Release **`v0.3.0`** cut (MINOR — `.rdb` binary save format is a new user-visible feature): `CHANGELOG.md` `[Unreleased]` → `[0.3.0] - 2026-09-04`, CMake `project(VERSION)` 0.2.0 → 0.3.0, tag `v0.3.0` pushed. Milestone `Sprint 11` closed. Table reset below for Sprint 12. **Outstanding:** manual live/visual WinGraph smoke of a real `.rdb` save→reopen (no display on the build host). |
| 2026-09-04 | 2 / 2 items | — | Sprint 12 opened (goal "Post-ANLZ-01 Analyze Mode fixes plus engine-subprocess cleanup on exit"): ANLZ-05, ENG-03 pulled from Backlog into Active — see `docs/sprint/current.md`. Milestone `Sprint 12` + labels `sprint:12` / `area:ANLZ` / `area:ENG`. Backlog now has 1 item (TOOL-02). Next: `/implement-task ANLZ-05`. |

| 2026-09-04 | 1 / 2 items | — | ANLZ-05 implemented on branch `anlz-05/analyze-mode-no-automove-allow-mid-search-moves` (pending merge). `MainWindow`-orchestration only: `maybeStartAutoMove()`'s idle body bails on `viewConfig().analyzeMode`; `scheduleAnalyzeModeRestart()` dropped its `isEnginesTurn(...)` early-return (engine's-turn position analysed too); `signal_move_clicked` handler calls `controller_.stopAnalysis()` before `makeMove()` when `isAnalyzing()`. `GameState::makeMove()` guard unchanged; no `revertEnginePlaysToOff()` / `MatchConfig` write; no `ViewConfig` field / Settings row / protocol change. `main_window.h` +`friend struct RanlsAnlz05Probe` (test seam). planning.md Q6-reversal section carries a dated implementation note. +`tests/test_anlz05_stop_then_move.cpp` (2 cases) +`tests/test_anlz05_no_automove_action.cpp` (1 case, real `MainWindow` + wire spy). `./build.sh` clean (3 known warnings); ctest 3/3 (`ranls-gui-tests` 183 / 2317; `ranls-gui-ui-tests` 21 / 140). `docs/fix-log/2026-09-04-anlz05-analyze-mode-no-automove-mid-search-click.md` + row. Manual live-engine smoke still needs a human. Not counted landed — pending merge. |

| 2026-09-04 | 1 / 2 items | — | **ANLZ-05 merged** to `main` — PR #15 squash `f3bad66`. Orchestrator re-verified independently on the branch before merge: `./build.sh` clean (3 known warnings), `ctest` 3/3 (`ranls-gui-tests` 183 / 2317, `ranls-gui-ui-tests` 21 / 140), the 3 new `ANLZ-05` test cases confirmed running + passing. No merge conflict. `current.md` status → Done. Sprint 12 down to 1 open item (ENG-03). |

| 2026-09-04 | 2 / 3 items | — | **ANLZ-06 filed** into Sprint 12 Active — regression against the just-merged ANLZ-05 (Stop drops a stone; a board click mid-search double-moves). `/systematic-debugging` Phase 1–2 done: root cause is `EngineController` relaying every engine coordinate line to `signal_engine_move` unconditionally, since `analyze()`'s `YXNBEST` request culminates in a bestmove line. Fix scoped to `EngineController` (`SearchIntent` gate) — `docs/todo/ANLZ-06-*.md` + `docs/instruction/ANLZ-06-*.md`. Sprint 12 back up to 2 open items (ANLZ-06, ENG-03). |

| 2026-09-04 | 1 / 3 items | — | **ANLZ-06 merged** to `main` — PR #16 squash `576b25a`. Orchestrator re-verified independently on the branch before merge: `./build.sh` clean, `ctest` 3/3 (`ranls-gui-tests`, `rel02-version-single-source`, `ranls-gui-ui-tests`), the 4 new `ANLZ-06` test cases (34/34 assertions) and `ANLZ-05` (15/15 assertions) confirmed passing; diff matched the instruction file's exact intent-gating shape. `current.md` status → Done. Sprint 12 down to 1 open item (ENG-03). |
| 2026-09-04 | 1 / 3 items | — | **ANLZ-07 filed to Backlog** (not pulled into Active) — user report against the just-merged ANLZ-06, same transcript: once the discarded coordinate is correctly not played, nothing stops `scheduleAnalyzeModeRestart()` re-arming on every Idle transition, so a quickly-converging search (forced mate in the report) busy-loops STOP/redump/search/discard at native CPU speed forever. `/systematic-debugging` Phase 1–2 done from the transcript alone; left in Backlog pending a design decision with the user (skip-if-unchanged vs. minimum restart interval vs. both) — `docs/todo/ANLZ-07-*.md` + `docs/instruction/ANLZ-07-*.md`. Sprint 12 still 1 open Active item (ENG-03); points unchanged. |
| 2026-09-04 | 2 / 4 items | — | **ANLZ-07 pulled from Backlog into Sprint 12 Active** (`/sprint add-task [ANLZ-07]`, user confirmed). Still **blocked** on the same open design decision (skip-restart-if-unchanged vs. minimum restart interval vs. both) — `/implement-task ANLZ-07` cannot start until that's resolved with the user. `current.md` dependency graph + table updated; `TODO.md` line moved Backlog→Sprint 12 Active. Sprint 12 now 2 open Active items (ENG-03, ANLZ-07). |
| 2026-09-04 | 2 / 4 items | — | **ANLZ-07 design resolved with the user**: skip-restart-if-unchanged only (compare the completed analysis-intent result — best move + eval — to the previous completed result on the same position; no minimum-interval backstop — a still-changing result means still-converging, not a bug to mask). `docs/todo/ANLZ-07-*.md` Scope + `docs/instruction/ANLZ-07-*.md` updated to the resolved shape; `current.md`/`TODO.md` status wording updated. Ready for `/implement-task ANLZ-07`. Points unchanged. |
| 2026-09-05 | 1 / 4 items | — | **ANLZ-07 merged** to `main` — PR #17 squash `82be450`. `/implement-task` dispatch: `EngineController` now caches the best-move+eval-text result of each completed analysis-intent search keyed to `currentPath()` and exposes `analysisConverged()`; `MainWindow::scheduleAnalyzeModeRestart(bool force = false)` skips re-arming when converged and not `force`, with `force=true` at the two genuine-restart call sites (`signal_board_changed`, `onToggleAnalyzeMode(true)`) so a cached result never suppresses analysing a position the user actually asked to (re)study. Orchestrator re-verified independently on the branch before merge: clean from-scratch build (only the 3 known pre-existing `-Wunused-function` warnings), `ctest` — `ranls-gui-tests` pass, `rel02-version-single-source` pass, `ranls-gui-ui-tests` 23/24 (new `test_anlz07_analyze_restart_convergence.cpp` 3/3 cases pass; the 1 failure, `UI-12` Move Log scroll-timing, independently confirmed pre-existing by rebuilding `main` at the pre-fix commit and reproducing the identical failure there), `test_anlz05_*`/`test_anlz06_*` re-verified green in isolation, `check-tracking-sync.js` clean, diff scope matched the instruction file exactly (code + tests + tracking files only). `current.md` ANLZ-07 → Done. Sprint 12 down to 1 open item (ENG-03). |

| 2026-09-05 | 0 / 4 items | — | **ENG-03 merged** to `main` — PR #20 squash `b8418c7`. `/implement-task` dispatch: `onQuit()`'s body factored into `MainWindow::requestGracefulClose()`, now also wired to a new `signal_close_request` handler (vetoes the first WM "X" close, `controller_.stopEngine()`, re-issues `close()` from the completion callback; `closeInFlight_` guards re-entrancy, leaning on `EngineController::stopEngine()`'s existing Stopping-state completion chaining); `EngineProcess::start()` refactored to `Gio::SubprocessLauncher` + `g_subprocess_launcher_set_child_setup()` arming `PR_SET_PDEATHSIG(SIGKILL)` on Linux (`#ifdef __linux__`, unchanged fallback elsewhere). Orchestrator re-verified independently before merge: clean from-scratch build, `ctest` — `ranls-gui-tests` 195/195 assertions (incl. new `test_eng03_pdeathsig.cpp`), `rel02-version-single-source` pass, `ranls-gui-ui-tests` 25/26 cases (new `test_eng03_close_request.cpp` 10/10 assertions pass; the 1 failure, `test_anlz05_no_automove_action.cpp`, independently confirmed pre-existing via the 2026-09-05 UI-14 fix-log note, not a regression). Manual live-engine/display smoke (WM X during real analysis, `kill -9` of the GUI PID) not run — no engine binary/display on the build host, documented as skipped rather than claimed passing. **All 4 Sprint 12 Active items now landed — sprint ready to close + cut a release.** |

| 2026-09-05 | 0 / 4 items | — | Sprint 12 **closed** — ANLZ-05, ANLZ-06, ANLZ-07, ENG-03 landed on `main` (PRs #15/#16/#17/#20); archived to `docs/sprint/archive/sprint-12.md`, nothing rolled over. Release **`v0.3.1`** cut (PATCH — Analyze Mode refinements + engine-subprocess safety net, no new user-visible feature): `CHANGELOG.md` `[Unreleased]` → `[0.3.1] - 2026-09-05` (also folding in the standalone PROTO-04 and UI-14 fixes merged the same window), CMake `project(VERSION)` 0.3.0 → 0.3.1, tag `v0.3.1` pushed. Milestone `Sprint 12` closed. Table reset below for Sprint 13 |

| 2026-09-06 | 2 / 2 items | — | Sprint 13 opened (goal "Open Protocol Extension (.ptc runtime commands) plus tracking-tool marker fix"): TOOL-02, PROTO-03 pulled from Backlog into Active — see `docs/sprint/current.md`. TOOL-02 detail file scaffolded (`docs/todo/TOOL-02-check-task-structure-markers.md`); PROTO-03 design synced to RESOLVED (planning.md header + stale `$currentPath` follow-up). Backlog now empty |
| 2026-09-06 | 1 / 2 items | — | **TOOL-02 merged** to `main` — PR #21 squash `7ceeeed`. `/implement-task` dispatch (Haiku 4.5): `BULLET_START_RE` / `TODO_LINE_RE` in `scripts/check-task-structure.js` widened to accept `🔲` / `🚧` alongside `✅`; capture-group indices unchanged. Orchestrator re-verified: `🔲` TOOL-02/PROTO-03 no longer false orphans, throwaway-orphan still caught, dedup path untouched, `check-tracking-sync.js --full` still exit 0. `check-task-structure.js` still exits 1 on the `⛔`-marked non-canonical ANLZ-03 lines — outside TOOL-02's `🔲`/`🚧` scope, split out as **TOOL-03** (Backlog). No test harness (standalone Node script) — verification is the manual acceptance-criteria runs |

| 2026-09-06 | 0 / 2 items | — | **PROTO-03 merged** to `main` — PR #22 squash `75245d9`. `/implement-task` dispatch (Sonnet 5, worktree): new standalone `src/engine/protocol_extension.{h,cpp}` (hand-rolled strict TOML-subset parser — no `third_party/` added; fail-closed Q3 collision check vs. the 18-name built-in registry, Q5 limits, Q2 `if/elif/else` DSL with 3 whitelisted sinks via dispatch table), new minimal `ICustomCommandSource` (not on `IEngineProtocol`) implemented by `GomocupProtocol` with built-in parsing running first and unchanged, `EngineController::loadExtensionTable()`/`sendCustomCommand()`/`signal_custom_action` (`customSource_` null on a `.ptc`-less run), `CommandDispatcher::syncExtensionCommands()` into the same `!` registry + `[extension]` help group, UI sinks reusing `EngineStatusView` dynamic-label box / crash-banner / `EngineLogModel`, `EngineConfig::protocolExtensionPath` + `*.ptc` Settings row. Orchestrator re-verified independently on the branch before merge: clean from-scratch build (only the 3 known pre-existing `-Wunused-function` warnings), `ctest` — `ranls-gui-tests` 209/209 cases / 2466 assertions (14 new `test_proto03_*` cases), `rel02-version-single-source` pass, `ranls-gui-ui-tests` 25/26 (the 1 failure `test_anlz05_no_automove_action` independently reproduced identically on branch base `8b010ed` — pre-existing host limitation, not a regression), diff scope matched the instruction file (code + tests + tracking only). `current.md` PROTO-03 → Done. Live-engine/display "Manual smoke" tier still pending a human. **Both Sprint 13 Active items (TOOL-02, PROTO-03) now landed — sprint ready to close + cut a release.** Board card move to Done deferred to the sprint-close batch (token lacks `project` scope). |

| 2026-09-06 | 0 / 2 items | — | Sprint 13 **closed** — TOOL-02, PROTO-03 landed on `main` (PRs #21/#22); archived to `docs/sprint/archive/sprint-13.md`, nothing rolled over. Release **`v0.4.0`** cut (MINOR — Open Protocol Extension is a new user-visible feature): `CHANGELOG.md` `[Unreleased]` → `[0.4.0] - 2026-09-06`, CMake `project(VERSION)` 0.3.1 → 0.4.0, tag `v0.4.0` pushed. Milestone `Sprint 13` closed. Also filed this window: PORT-01/02/03 (platform-dependency audit follow-up) into Backlog. Table reset below for Sprint 14 |

| 2026-09-06 | 2 / 2 items | — | Sprint 14 opened (goal "Backlog tooling fix + Tier-1 Windows portability"): TOOL-03, PORT-01 pulled from Backlog into Active — see `docs/sprint/current.md`. Milestone `Sprint 14` (#9) + labels `sprint:14` / `area:PORT` / `area:TOOL`. Backlog now has 2 items (PORT-02, PORT-03). Next: resolve TOOL-03's small format decision, then `/implement-task TOOL-03` / `/implement-task PORT-01` |
| 2026-09-06 | 0 / 2 items | — | Sprint 14 **closed** — TOOL-03, PORT-01 landed on `main` (PRs #23/#24); archived to `docs/sprint/archive/sprint-14.md`, nothing rolled over (PORT-01's MSYS2 MINGW64 smoke is an outstanding human step, not rolled scope). Release **`v0.4.1`** cut (PATCH — TOOL-03 + PORT-01 are fix/polish, no new user-visible feature on the shipping Linux platform): `CHANGELOG.md` `[Unreleased]` → `[0.4.1] - 2026-09-06`, CMake `project(VERSION)` 0.4.0 → 0.4.1, tag `v0.4.1` pushed. Milestone `Sprint 14` (#9) closed. Also this window: native MSVC / Windows CI confirmed a goal (audit `2026-09-06-native-msvc-windows-ci-goal.md`), un-gating PORT-03. Table reset below for Sprint 15 |
| 2026-09-06 | 2 / 2 items | — | Sprint 15 opened (goal "Tier-2 Windows portability: bundled assets + MSVC/CI harness"): PORT-02, PORT-03 pulled from Backlog into Active — see `docs/sprint/current.md`. Milestone `Sprint 15` + labels `sprint:15` / `area:PORT`. Backlog now empty. Next: `/implement-task PORT-02` (land first — CMake ordering), then `/implement-task PORT-03` |
| 2026-09-06 | 1 / 2 items | — | **TOOL-03 merged** to `main` — PR #23 squash `69e10e0`. `/implement-task` dispatch (Haiku 4.5): modified `BULLET_START_RE` / `TODO_LINE_RE` in `scripts/check-task-structure.js` to add `⛔\s*` to the marker alternation and make ` SUPERSEDED` optional (non-canonical format for the `⛔`-marked ANLZ-03 item). Orchestrator re-verified independently: `node scripts/check-task-structure.js` exit 0, `check-tracking-sync.js --full` still exit 0, orphan detection / duplicate detection unchanged. `current.md` TOOL-03 → Done. Remaining: PORT-01 |

| 2026-09-06 | 0 / 2 items | — | **PORT-01 implemented** on branch `port-01/cross-platform-build-and-runtime` (not yet merged — orchestrator drives the PR + board sync). `/implement-task` dispatch (Sonnet 5): three isolated `#ifdef` guards — `<unistd.h>` guard + Windows exec-extension check in `settings_dialog.cpp`, `::_commit` alongside `::fsync` in `rdb_container.cpp`, `GetModuleFileNameW` / `_NSGetExecutablePath` branches in `settings_storage.cpp::executableDir()`. No Linux behaviour change; Linux ctest unchanged vs clean `main` (`ranls-gui-tests` 209/209, `ranls-gui-ui-tests` 28/29 — same lone pre-existing `test_anlz05_no_automove_action` flake). +`tests/test_port01_engine_path_check.cpp`. `_WIN32`/`__APPLE__` branches carry per-guard correctness reasoning in the fix-log; MSYS2 MINGW64 smoke still a required human step. `current.md` PORT-01 → Done. Sprint 14 work complete pending merge + smoke. |

| 2026-09-06 | 0 / 2 items | — | **PORT-01 merged** to `main` — PR #24 squash `4edb0a2`. Orchestrator re-verified independently on the branch: clean build no new warnings; `ranls-gui-tests` 209/209 (2466 assertions); `ranls-gui-ui-tests` 28/29 — the lone `test_anlz05_no_automove_action` failure reproduces identically on a clean `main` (`c90b0fd`) build (25/26), Linux ctest unchanged. `current.md` PORT-01 → Done (PR #24). **Both Sprint 14 Active items (TOOL-03, PORT-01) now merged** — sprint work complete; PORT-01's MSYS2 MINGW64 build+launch+save-`.rdb`+pick-engine smoke remains a required human step. Ready for the `github` skill "Cutting a release" checklist (`v0.5.0`). |

| 2026-09-06 | 1 / 2 items | — | **PORT-02 merged** to `main` — PR #25 squash `328490e`. `/implement-task` dispatch (Sonnet 5, worktree): new `src/resources/ranls.gresource.xml` (`/org/ranls/` prefix); `CMakeLists.txt` locates `gio-2.0` + `glib-compile-resources`, `add_custom_command` compiles the bundle to `build/generated/ranls_gresource.c` (build-dir only) linked into `ranls-gui`, removed the `POST_BUILD` `style.css` copy + `install(FILES src/resources/style.css …)`; `src/application.cpp::loadStylesheet()` → `CssProvider::load_from_resource("/org/ranls/style.css")` in `try/catch(const Glib::Error&)`, dropped `<filesystem>` + the CWD probe + the `__FILE__` build-host-path fallback. +`tests/port02_check_gresource.cmake` (new CTest `port02-style-css-bundled` — binary-inspection guard: CSS resource path linked in, no build-host path baked in). Orchestrator re-verified independently on the branch: clean from-scratch build (only the 3 known pre-existing `-Wunused-function` warnings), `strings ranls-gui | grep -c /run/media` → 0, `grep -c org/ranls/style.css` → 1; `ctest` — `port02-style-css-bundled` + `ranls-gui-tests` + `rel02-version-single-source` pass, `ranls-gui-ui-tests` fails 2 cases (`test_anlz05_no_automove_action`, `test_ui12_move_log_scroll_target`) both documented pre-existing flakes (UI-14 / ANLZ-07 fix-log), unreachable by a CSS/CMake change. `current.md` PORT-02 → Done. From-any-directory styled-launch smoke (`strings` empty + visible styling) still a required human step (no display server on build host). Remaining: PORT-03. Board card move to Done deferred to the sprint-close batch (token lacks `project` scope). |

| 2026-09-06 | 0 / 2 items | — | **PORT-03 merged** to `main` — PR #26 squash `38ce332`. `/implement-task` dispatch (Sonnet 5, worktree): `CMakeLists.txt` `if(MSVC) /W4 /permissive- else() -Wall -Wextra -Wpedantic` + `WIN32` on `add_executable(ranls-gui …)` + gtkmm/gio discovery gated `if(MSVC)`→`find_package(PkgConfig)` shim (else branch unchanged) + README "Building on Windows"; new `tests/mock_engine/` (targets `mock_engine` cat-like / `mock_engine_quit` true-like, paths as `MOCK_ENGINE_PATH`/`MOCK_ENGINE_QUIT_PATH` compile defs), 7 suites migrated off `/bin/cat`+`/bin/true` with assertions untouched, `test_eng03_pdeathsig.cpp` left Linux-guarded; `engine_process.cpp` `#elif defined(_WIN32)` Job Object (`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`) leaving the Linux PDEATHSIG path + macOS fallback untouched. Also fixed a pre-existing `BottomPanel` deferred-scroll UAF the migration made deterministic (bare `this` captured in 4 idle slots → crash in `scroll_to` after teardown; same defect as the "ui12 scroll" flake) — `sigc::track_obj`-bound + regression case in `test_ui10_engine_log_scroll_target.cpp`. Orchestrator re-verified independently on the branch: clean from-scratch build (only the 3 known `-Wunused-function` warnings), `ctest` — `port02` + `ranls-gui-tests` 209/209 (2466 assertions) + `rel02` pass, `ranls-gui-ui-tests` 29/30 (lone failure `test_anlz05_no_automove_action` reproduced 5/5 on baseline `670d0dc`, no crash, 0 skipped), no `/bin/cat`/`/bin/true` left in test code. `current.md` PORT-03 → Done. Scope item 4 (Windows CI job) deferred as optional. Windows tier (native MSVC compile, no console window, `taskkill /f` orphan check) + MSYS2 MINGW64 rebuild remain a required human smoke test. **Both Sprint 15 Active items (PORT-02, PORT-03) now landed — sprint ready to close + cut a release.** Board card move to Done deferred to the sprint-close batch (token lacks `project` scope). |

| 2026-09-06 | 1 / 3 items | — | **UI-15 pulled from Backlog into Sprint 15 Active** (mid-sprint) — the recurring "ui12 scroll flake": `BottomPanel::scrollMoveLogToEnd()` never got UI-14's direct `vadj->set_value` kinetic-animation snap. `/systematic-debugging` phases 1–3 already done. Sprint 15 item count 2 → 3, remaining 0 → 1. Unrelated to the Windows-portability goal — scoped to `src/ui/bottom_panel.cpp` + `test_ui12`. Backlog unchanged otherwise. Next: `/implement-task UI-15`, then `/sprint close`. |

| 2026-09-06 | 0 / 3 items | — | **UI-15 implemented** on branch `ui-15/move-log-scroll-races-kinetic-animation` (not yet merged — orchestrator drives the PR + board sync). `scrollMoveLogToEnd()` now mirrors `scrollEngineLogToBottom()`: direct `scrolledMoveLog_` vadjustment snap (`if (maxValue > value) set_value(maxValue)`) after both the immediate `scroll_to` and the `sigc::track_obj` idle re-issue; persistent mark + idle unchanged. `test_ui12_move_log_scroll_target` fixed `pump(300)` → condition-based wait (pump in 10ms steps until at-bottom or a 3000ms deadline), case kept as the regression guard. No change to `programmaticScroll_`/`stickToBottom_`, the Engine Log path, RT-02, or the UI-05 gutter. Build clean (no new warnings); `test_ui12` 20/20 consecutive direct-binary runs under `$(nproc)`-wide CPU load + 5/5 under `ctest` load; `ranls-gui-ui-tests` 30/30, `ranls-gui-tests` 209/209, `ctest` 4/4 (`port02`/`rel02` incl.). `check-task-structure.js` + `check-tracking-sync.js` pass. `current.md` UI-15 → Done. |

To render this as a chart, ask for it explicitly (Artifact, on request — see `/CLAUDE.md` "Sprint
cadence": don't auto-render on every update).
