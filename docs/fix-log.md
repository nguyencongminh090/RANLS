# Fix log

Append-only index of bug fixes, whether or not they started as a tracked `TODO.md` item. One new
`docs/fix-log/<date>-<slug>.md` detail file per fix, one new row here. Never edit, reorder, or
delete an existing row — a wrong past entry gets a new correcting entry, not a rewrite.

| Timestamp | Summary | Detail |
|---|---|---|
| 2026-08-21 | Stale PV/engine-status/board markers survived New Game, makeMove, undo, redo, gotoMove, gotoPath (STATE-01) | [detail](fix-log/2026-08-21-state-01-stale-analysis-after-position-change.md) |
| 2026-08-21 | Hardened Gomocup parser against malformed engine output: OOB `currentPVs_[-1]`, unbounded `NUMPV` resize, unvalidated database coords (PROTO-01) | [detail](fix-log/2026-08-21-proto-01-parser-hardening.md) |
| 2026-08-21 | Coalesced `GameState::signal_engine_analysis` onto a ~75ms UI timer with flush-on-search-completion instead of emitting once per parsed engine line (up to 8x/depth with multiPV=8); cached `evalHistory()` (RT-01) | [detail](fix-log/2026-08-21-rt-01-throttle-analysis-signal.md) |
| 2026-08-21 | `PVView::update()` now reuses row widgets in place instead of destroying/recreating every row (and its hover controller) per analysis update, fixing the PV-hover ghost-stone preview flicker (RT-03) | [detail](fix-log/2026-08-21-rt-03-pvview-rebuild-breaks-hover.md) |
| 2026-08-21 | Engine Log now uses one tagged-prefix `TextView` (removing the gutter/content wrap desync) backed by a bounded, batched `EngineLogModel` instead of unbounded per-line inserts (RT-02) | [detail](fix-log/2026-08-21-rt-02-engine-log-unbounded.md) |
| 2026-08-21 | Table tab now click-to-jumps and highlights the current row (switched `Gtk::NoSelection`→`SingleSelection`, wired `signal_node_selected`→`gotoPath`, added current-row CSS), and the two tree tabs are labelled to show they render different datasets (UI-02) | [detail](fix-log/2026-08-21-ui-02-tree-table-tab-click-to-jump-and-highlight.md) |
| 2026-08-21 | `SettingsDialog::onApply` now copies from the config it was opened with instead of default-constructing, so `customParams`/`showDatabase` survive Apply; added a `multiPV` control and disk persistence for `customParams` (STATE-02) | [detail](fix-log/2026-08-21-state-02-settings-dialog-drops-config-fields.md) |
| 2026-08-21 | `mainHPaned_`/`mainVPaned_` divider positions are now tracked as a fraction of the pane's own extent and reasserted on every window allocation, so shrinking the window and growing it back restores the board pane instead of leaving it clamped to a sliver (UX-05) | [detail](fix-log/2026-08-21-paned-resize-does-not-restore.md) |
| 2026-08-21 | Replaced `EngineController`'s `started_`/`analyzing_` bool pair with an explicit `EngineState` enum (honoring `EngineProcess::start()`'s return value, distinct crashed/thinking states, active crash banner) and made engine shutdown fully non-blocking, removing both `g_usleep` waits and the re-entrant `g_main_context_iteration` pump from the stop path (ENG-01) | [detail](fix-log/2026-08-21-eng-01-engine-state-honesty-and-blocking-stop.md) |
| 2026-08-21 | `parseEngineCoord` now takes the real board size instead of a hardcoded 15 (was corrupting parsed move data on non-15×15 boards); `EngineStatusView`'s "Best:" readout and `BoardRenderer`'s star points now use the real board size too; `sendConfig()` refreshes `GomocupProtocol::boardSize_` even while the engine is stopped (PROTO-02) | [detail](fix-log/2026-08-21-proto-02-hardcoded-board-size-15.md) |
| 2026-08-21 | `GomocupProtocol::commitPV` now truncates `currentPVs_` to size 1 when a new round's PV index 0 arrives while stale higher-index entries remain from a larger previous round (multiPV lowered mid-search); `PVView::update` filters out any empty-move `PVLine` before rendering (STATE-03) | [detail](fix-log/2026-08-21-state-03-currentpvs-never-shrinks.md) |
| 2026-08-21 | Split `undoMove()`/`redoMove()` into silent position-mutation halves so `undoAll`/`redoAll`/`gotoMove` do exactly one `clearDatabase()`/`signal_board_changed` for the whole bulk op instead of once per ply, and wired up the previously-dead `signal_move_selected` in `gotoMove()` (NAV-01) | [detail](fix-log/2026-08-21-nav-01-bulk-navigation-flooded-engine-and-ui.md) |
| 2026-08-21 | Win-rate graph: fixed off-by-one side-to-move attribution, eval writeback wrongly gated on depth/nodes only (dropped same-depth score revisions), and unevaluated nodes indistinguishable from a true 50%; confirmed mate scores already render sanely at both extremes (no change needed) (UI-01) | [detail](fix-log/2026-08-21-ui-01-winrate-attribution-errors.md) |
| 2026-08-21 | Board-size investigation (5-22): confirmed and fixed coordinate-label clipping at 5x5 (unbounded font size, no ceiling) and 3-digit move-number overflow at 22x22 (font size unrelated to digit count); unified the duplicated board-geometry formula into `BoardRenderer::computeGeometry()`, shared by `draw()` and `BoardView::pixelToCoord()`; star points at 5x5 and the 5-22 size cap confirmed already correct (UX-04) | [detail](fix-log/2026-08-21-ux-04-board-geometry-and-overflow.md) |
| 2026-08-21 | `GameState::setAnalysisData` no longer emits `signal_tree_updated` synchronously per parsed engine line; coalesced onto RT-01's tick/flush via a new `treeDirty_` flag, `TreeExplorer::update` diffs rows into its `Gio::ListStore` with `splice()` instead of `remove_all()`+re-append, and `TreeNodeView::layoutTree` threads the parent path/index down through recursion instead of scanning `nodes_` for it, making layout O(n) instead of O(n²) (RT-04) | [detail](fix-log/2026-08-21-rt-04-tree-views-full-rebuild.md) |
| 2026-08-21 | Engine path in Settings is now validated live (exists/regular file/executable) with an inline status label under the field and Apply desensitized while invalid, instead of applying an unchecked path silently (UX-02) | [detail](fix-log/2026-08-21-ux-02-settings-dialog-engine-path-validation.md) |
| 2026-08-21 | Fixed 3 leaked `new`-allocated dialogs in `main_window.cpp` (delete-on-hide pattern), removed leftover `[DBG]` debug output and an unused local, unified duplicated `kCoordMargin` constant into `board_geometry.h`; confirmed the two dead-signal items were already wired up by NAV-01/UI-02 (CLEAN-01) | [detail](fix-log/2026-08-21-clean-01-dialog-leaks-and-dead-code.md) |
| 2026-08-21 | Added a persistent header-bar rule indicator, a new `RenjuRule` domain module (`src/model/renju_rule.h`/`.cpp`) marking Black's forbidden points (overline/double-four/double-three) on the board without blocking the click, made `BoardState::checkWin()` rule-aware so Standard/Renju overline handling matches the engine's own win condition, and connected the previously-dead `signal_rule_changed` (UI-03) | [detail](fix-log/2026-08-21-ui-03-rule-not-visible-on-board.md) |
| 2026-08-21 | Added a shared `EmptyState`/`EmptyStateOverlay` helper so `WinGraphView`, `PVView`, `TreeNodeView`, `TreeExplorer`, and `BottomPanel`'s Move/Engine logs show a specific placeholder (and `WinGraphView` still draws its axis scaffold) instead of a blank rectangle when they have no data (UX-01) | [detail](fix-log/2026-08-21-ux-01-empty-states.md) |
| 2026-08-30 | PV list stopped accumulating rows across positions / duplicate `PV #1` at MultiPV=1: extracted `GomocupProtocol::clearAnalysisState()` and wired it to `GameState::signal_board_changed` via `EngineController`, and made `EngineController` drop `signal_analysis` while `!isAnalyzing()` so trailing post-stop engine lines can't repopulate stale PVs (UI-04) | [detail](fix-log/2026-08-30-ui04-pv-view-appends-across-positions.md) |
| 2026-08-30 | Implemented Load Game / Save Game (were empty no-op stubs): new gtkmm-free `src/model/game_io.{h,cpp}` versioned `key=value` + `move=x,y` serializer (settings_storage style, no JSON), `Gtk::FileDialog` open/save pickers, discard-confirmation on Load, self-deleting ERROR dialog on corrupt input, model rebuilt via newGame/setRule/replay/sendConfig (IO-01) | [detail](fix-log/2026-08-30-io01-load-save-game.md) |
| 2026-08-30 | PV panel carried a stale row into the next position's analysis (real compact `MESSAGE depth …` format; UI-04 missed it). Protocol layer was clean — root cause was `AnalysisPanel`'s `signal_board_changed` handler refreshing the win-graph/tree views but never `pvView_`/`engineStatus_`, so PV clearing depended on `GameState::resetAnalysisState()` emitting `signal_engine_analysis`, which it skips (RT-01 `alreadyEmpty` guard) when the model is already empty. Handler now refreshes `pvView_`/`engineStatus_` straight from `gameState_` on every position change (pvLines() is guaranteed empty by then). +2 real-format regression tests (UI-07) | [detail](fix-log/2026-08-30-ui07-pv-panel-accumulates-across-positions.md) |
| 2026-08-21 | Icon-only nav buttons now have tooltip + accessible label; New Game and board-size Apply confirm before discarding a non-empty game; fixed a real coordinate-label/board-background contrast failure (~1.55:1→~8.0:1) and a database-marker-label contrast failure, paired win-graph series color with a dash pattern, and made win-graph axis labels theme-aware (UX-03) | [detail](fix-log/2026-08-21-ux-03-accessibility-and-destructive-actions.md) |
| 2026-08-30 | Engine Log direction tag ([SEND]/[MESSAGE]/[OUTPUT]/…) moved out of the copyable text into a sibling fixed-width `Gtk::DrawingArea` gutter (`engine-gutter`) that paints each tag at its buffer line's live y-position; the `TextView` buffer now holds payload only, so selecting rows and copying yields raw engine text with no prefixes. RT-02 bounded/batched semantics and the `EngineLogModel` tag source-of-truth unchanged (UI-05) | [detail](fix-log/2026-08-30-ui05-engine-log-gutter-column.md) |
| 2026-08-30 | PV panel STILL accumulated a stale row per position after the first UI-07 fix — real root cause was one layer below: `PVView::update()`'s shrink path called `Gtk::ListBox::remove()` with the row's inner `Gtk::Box`, a *grandchild* of the list box (append() implicitly wraps it in a `GtkListBoxRow`), which GTK 4 rejects with "Tried to remove non-child" and ignores — leaking one orphan row widget per clear while `rows_.size()` correctly went to zero, so the next analysis appended below the stale row. `PVView` now creates the `Gtk::ListBoxRow` explicitly and removes that. Also fixed MultiPV N→M shrink leaving N rows. New gtkmm-linking test binary `rapfi-gui-ui-tests` asserts the rendered widget tree; reproduced + verified against the real `pbrain-rapfi` (UI-07, second pass — continues/corrects the entry above, which is left as written) | [detail](fix-log/2026-08-30-ui07-pvview-listbox-orphan-rows.md) |
| 2026-08-30 | Selected rule and board size were never written to the settings file, so every launch reverted to Freestyle / size 15 (found during UI-06's smoke pass; not a UI-06 regression). New `GameSetupConfig` (rule + boardSize) in `config.h`, threaded through `SettingsBundle` + a 4th `save()` param exactly like UI-06's `MatchConfig`; `load()`/`save()` round-trip `rule` (0/1/2 else Freestyle) and `board_size` (5..MAX_BOARD_SIZE else 15). `MainWindow` restores both at startup (setRule + newGame on the empty board), persists via a new `persistGameSetup()` from `onSetRule()` and the Board Size Apply handler, and `onNewGame()` keeps the current size instead of snapping to DEFAULT_BOARD_SIZE. All three `save()` call sites pass the full four-block state (STATE-02 hazard). +4 round-trip tests (STATE-04) | [detail](fix-log/2026-08-30-state04-rule-and-board-size-not-persisted.md) |
| 2026-08-31 | Removed the UX-01 idle/no-data placeholder text ("No analysis yet — press Analyze (F5)", "No moves yet — …", "start the engine to see log output", …) from the PV view, move log, engine log, both tree views, and WinGraphView — empty panels now render clean. WinGraphView's 0/50/100% axis scaffold is kept (structural, not instructional — confirmed with the user). `EmptyState::drawPlaceholder()` helper + `.empty-state-message` CSS removed; `EmptyStateOverlay` reduced to an inert passthrough wrapper (no label created/measured, `setEmpty()` a no-op) so no panel layout changes. STATE-01 clear/notify path untouched; UI-09 out of scope. +2 widget-tree regression tests (`test_ui08_no_empty_state_text.cpp`) (UI-08) | [detail](fix-log/2026-08-31-ui-08-remove-empty-state-placeholder-text.md) |
| 2026-08-30 | Menu-bar "Analysis" submenu (Analyze/Stop — duplicated the toolbar) replaced with an "Engine plays" radio group (Black/White/Off). New `MatchConfig` (`EnginePlaysSide`) in `config.h`, threaded through `GameState` + `settings_storage` (`engine_plays` key, out-of-range→Off). New auto-move path: `IEngineProtocol::generateMoveRequest()` → `GomocupProtocol` `BOARD …/DONE`/`BEGIN` starting a real search, `EngineController::requestEngineMove()`, `MainWindow::maybeStartAutoMove()` gated on side-to-move + engine Idle (no loop). Toolbar Analyze/Stop untouched. +protocol & settings round-trip tests. User-verified via live-engine smoke; closed by the user (STATE-04 surfaced from the same pass) (UI-06) | [detail](fix-log/2026-08-30-ui06-engine-plays-menu.md) |
| 2026-08-31 | Interrupting engine auto-play now reverts "Engine plays" to Off (transient, in-memory only — no `SettingsStorage::save`, persisted side restored next launch). New pure predicate `isEnginesTurn(EnginePlaysSide, Stone)` in `config.h` (also now used by `maybeStartAutoMove`); new `MainWindow::revertEnginePlaysToOff()` called from `onStopAnalysis` (toolbar + hotkey Stop), the analysis-panel `signal_stop` lambda, and `onStartAnalysis` when it is the engine's assigned turn; `CommandDispatcher::revertEnginePlaysIfEnginesTurn()` at the `analyze` + `!play` sites; menu radio re-synced via a `syncEnginePlaysMenu()` added to the `signal_config_changed` handler (state-only `change_state`, no `onSetEnginePlays` re-entry). Plain board moves on the engine's turn deliberately excluded. +`test_eng02_revert_predicate.cpp` (3 cases). Build clean, both ctest suites pass (ENG-02) | [detail](fix-log/2026-08-31-eng-02-interrupting-engine-auto-play-reverts-to-off.md) |
| 2026-08-31 | Single-sourced the app version, which disagreed across `CMakeLists.txt` (`1.0.0`), `onAbout()` (`"2.0"`), and git (no tags). The one literal is now `project(rapfi-gui VERSION 0.1.0)` (adopts REL-01's `v0.1.0`); `configure_file(src/version.h.in → build/generated/version.h, @ONLY)` emits `#define APP_VERSION`, and `build/generated` is on `rapfi-gui`'s include path. `src/main.cpp` scans argv for `--version`/`-v` at the very top of `main()` — before `RapfiApplication::create()` / any GTK-Gio init — prints `APP_VERSION`, `return 0`; `onAbout()` `set_version("2.0")` → `set_version(APP_VERSION)`. No arg-parsing dependency. `game_io.cpp` `kFormatVersion`/`yxgame_version` left untouched (save-file schema). New script-based ctest `rel02-version-single-source` (`tests/check_version.cmake`, links no gtkmm) runs the real binary headless and asserts `--version`/`-v` == CMake `PROJECT_VERSION`. Build clean; ctest 3/3; `env -u DISPLAY -u WAYLAND_DISPLAY ./build/rapfi-gui --version` → `0.1.0` rc=0, no window (REL-02) | [detail](fix-log/2026-08-31-rel-02-version-string-single-source.md) |
| 2026-08-31 | WinGraph `SingleSide` mode is now unconditionally Black's perspective — removed the UX-06 coupling that made it follow `MatchConfig::enginePlays` (ambiguous without checking Settings, and could flip mid-session via ENG-02's revert-to-Off). `buildWinGraphSeries` drops the `whitePerspective` branch (param kept, `(void)`-discarded, pinned by a rewritten regression case); `config.h` / `win_graph_series.h` doc comments + Settings combo label updated. Win-rate line thickened (main 1.5→2.8 px, White 1.2→2.6 px, round caps) and recoloured blue `#1A73E8` + green `#1E8E3E` — clears WCAG 3:1 non-text contrast on both the light (`#fafafb`, 4.32/4.03:1) and dark (`#242424`, 3.45/3.69:1) Adwaita panel backgrounds (old yellow White line was 1.59:1 on light); White line keeps its dash for CVD-safe shape redundancy; `dataviz` validator all-PASS both modes. Scaffold left subordinate. Build clean, 132 + 6 tests pass (UI-09) | [detail](fix-log/2026-08-31-ui-09-wingraph-single-side-black-and-thicker-line.md) |
| 2026-08-31 | Engine Log now stays pinned to the newest line during fast streaming analysis (UI-10). Primary cause: `scrollToEnd()` created an end-of-buffer `Mark`, called GTK4 `scroll_to` (which *defers* the scroll past the next relayout when the TextView height isn't validated — always so right after a batched insert), then `delete_mark`'d it on the same line, dropping the deferred scroll's target; the view only caught up on ticks where layout was already current. Compounding: `isScrolledToBottom()` re-checked `value+page_size>=upper-1` against a mid-stream stale `upper`, latching `wasAtBottom` false so auto-scroll disabled itself for the rest of the run. Fix: one persistent right-gravity `engineLogEndMark_` (created once, never deleted) that `flushPending` scrolls to and re-issues once on `Glib::signal_idle` after relayout; plus a remembered `stickToBottom_` intent flipped in exactly one place (the vadjustment `value_changed` settle handler) so a lone stale geometry read can't kill stickiness and a scrolled-up user is never yanked down. Decision logic extracted to pure `src/ui/sticky_scroll.h` + `tests/test_ui10_sticky_scroll.cpp` (5 cases, incl. the stale-`upper` regression). Move Log `scrollToEnd()` path, RT-02 buffer cap, UI-05 gutter, flush cadence all untouched. Build clean, ctest 3/3. Manual live-engine streaming test NOT run — no engine binary on this machine; scroll-plumbing half rests on GTK4 API semantics, decision half is unit-tested (UI-10) | [detail](fix-log/2026-08-31-engine-log-sticky-bottom.md) |
| 2026-08-31 | Engine Log gutter tags (`SEND`/`MESSAGE`/…) now paint in the log `TextView`'s own resolved font (`engineLogView_.get_pango_context()->get_font_description()`) instead of a hardcoded `"Monospace 11"`, so a tag matches the family and size of the row it labels; the one-time column-width probe uses the same font. `drawGutter()` only — no `EngineLogModel` / UI-10 scroll / UI-05 clipboard change. Build clean, ctest 3/3. Small user request, no TODO (manual visual check pending — no engine binary on the build machine) | [detail](fix-log/2026-08-31-gutter-font-matches-log.md) |
| 2026-08-31 | UI-10 follow-up — PR #2 did NOT fix the reported bug (Engine Log still pinned to the FIRST line during analysis). Real root cause: the Engine Log `TextView` was wrapped in `EmptyStateOverlay` (a `Gtk::Overlay`, no-op passthrough since UI-08) as the `Gtk::ScrolledWindow`'s child; `Gtk::Overlay` is not `Gtk::Scrollable`, so GTK interposed an implicit `GtkViewport` and every `engineLogView_.scroll_to(...)` — including PR #2's persistent-mark call — was a silent no-op. Fix: `scrolledEngineLog_.set_child(engineLogView_)` directly (TextView is `Gtk::Scrollable`); plus a `programmaticScroll_` guard so the `value_changed` handler ignores our own scroll + the RT-02 front-trim and can't latch `stickToBottom_` off now that the adjustment really moves. PR #2's mark + `sticky_scroll.h` helpers kept. New real-widget regression cases in `rapfi-gui-ui-tests` (`test_ui10_engine_log_scroll_target.cpp`, 3 cases: direct-child TextView; 400 streamed lines end pinned to bottom; parked-at-top view not yanked down) — all fail pre-fix. Move Log has the same latent no-op, left as a follow-up CODE. Build clean, ctest 3/3 | [detail](fix-log/2026-08-31-engine-log-still-not-sticky-wrong-scroll-widget.md) |
| 2026-08-31 | Move Log now auto-scrolls to the newest move (UI-12) — the same `Gtk::Overlay`-breaks-`Gtk::Scrollable` no-op UI-10's second pass fixed for the Engine Log. `scrolledMoveLog_.set_child(moveLogView_)` directly (the `TextView` is `Gtk::Scrollable`) instead of via `EmptyStateOverlay` (which forced an implicit `GtkViewport` that swallowed every `scroll_to`); static `scrollToEnd()` replaced by non-static `scrollMoveLogToEnd()` scrolling to a persistent right-gravity `moveLogEndMark_` re-issued once on idle (a plain immediate scroll + immediate mark-delete lands short during a burst append, e.g. a saved game replay). `appendMoveLog` always follows to the bottom — the "don't yank a scrolled-up user" behaviour is deliberately out of scope (UI-10's remembered-intent machinery doesn't fall out cleanly for a burst-appended log; the naive pre-append check self-disables). Cleanup: removed the now-purposeless `moveLogOverlay_`/`engineLogOverlay_` members, `updateMoveLogEmptyState()`/`updateEngineLogEmptyState()` + call sites, and the unused `empty_state.h` include (the `EmptyStateOverlay` type stays — used by `pv_view.h`/`tree_explorer.h`). UI-10 Engine Log path, RT-02 cap, UI-05 gutter untouched. +2 real-widget regression cases (`test_ui12_move_log_scroll_target.cpp`), both fail pre-fix. Build clean, ctest 3/3 (1081 + 70 assertions). No live-engine binary — manual GUI streaming check not possible (same as UI-10) | [detail](fix-log/2026-08-31-ui-12-move-log-not-sticky-to-bottom.md) |
| 2026-08-31 | Help → About rewritten (UI-11): the bare `Gtk::AboutDialog` (wrong name "Rapfi Analysis", no info, stock layout) replaced by a custom `AboutDialog` class (`src/ui/about_dialog.{h,cpp}`) — two-pane layout (themed stock icon left; right column: `RANLS` `.title-1` heading, `Version <APP_VERSION>`, and three titled sub-sections: Developer credit `Developer: Nguyen Minh` + copyright, a Tech/build-info `Gtk::Grid` (GTK/gtkmm/Cairo runtime versions, build date, git commit, license), and Links & protocol (`<a href>` markup labels for the repo + Gomocup/Yixin protocol, plus the supported-engine list)); single bottom-right Close button, Esc closes. `MainWindow::onAbout()` shrinks to the CLEAN-01 heap + `set_hide_on_close` + `signal_hide→delete` lifetime. New `src/build_info.h.in` → `build/generated/build_info.h` (mirrors `version.h.in`): `APP_BUILD_DATE` from `string(TIMESTAMP … UTC)`, `APP_GIT_COMMIT` from configure-time `git rev-parse --short HEAD` guarded to `"unknown"` for a no-`.git` build. `APP_VERSION` still the single CMake `project(VERSION)` literal (REL-02) — no hard-coded version. No hard-coded colours (built-in `.title-1`/`.dim-label` only). App-wide rename to `RANLS` (window title, `style.css`, app id) deliberately out of scope → split as NAME-01; no binary logo asset committed. +`tests/test_ui11_about_dialog.cpp` (3 real-widget cases in `rapfi-gui-ui-tests`: label tree incl. version/name/developer, modal+transient, builds under Adwaita + Adwaita-dark). Build clean, ctest 3/3 (`rapfi-gui-ui-tests` 13 cases / 81 assertions). Manual light/dark visual check of the running app left for the orchestrator | [detail](fix-log/2026-08-31-ui-11-about-window-rewrite.md) |
| 2026-09-03 | WinGraph left NaN gaps on plies the engine scored only implicitly (UI-13). `GameState::setAnalysisData` wrote `bestPv.score` onto the search-root node only; the position after `bestPv.moves[0]` was never touched, so under "Engine plays <side>" the reply ply stayed at the UI-01 NaN sentinel. Fix A: `setAnalysisData` now also fills that child node — when it already exists on the played line (never fabricated) and has no analysis of its own — with the complementary win% (`1 - score`, side to move flips) at a derived `depth = max(bestPv.depth-1, 1)`, behind the same "only if changed" guard and the existing `treeChanged`→`invalidateEvalHistoryCache()`/`treeDirty_` path. Plus: `EngineController::signal_move` reordered to `setAnalyzing(false)` → `flush()` → `signal_engine_move` → `setState(Idle)` so the searched position's final analysis is delivered before the board advances and before any `signal_state_changed` handler runs (RT-01 unchanged: one immediate flush on completion). eval→win% maths, UI-01 attribution, `buildWinGraphSeries`, UI-09 decoupling, RT-01 cadence, graph drawing all untouched. Candidates B/C and "evaluate the whole line" deferred (Backlog follow-up noted). +`tests/test_ui13_wingraph_eval_coverage.cpp` (4 model-layer cases). Build clean (no new warnings); ctest 3/3 (model 141 cases / 1112 assertions, UI 14 cases) | [detail](fix-log/2026-09-03-wingraph-record-eval-regardless-of-side.md) |
| 2026-09-03 | App-wide rename `"Rapfi Analysis"` → `RANLS` (NAME-01) — the leftovers UI-11 scoped out. Window title now `set_title(kAppDisplayName)` (new `inline constexpr` in `main_window.h`); `style.css` header comment `Rapfi Analysis GUI` → `RANLS GUI`; GTK application id `com.rapfi.analysis` → `com.ranls.gui` and stderr prefix `[Rapfi GUI]` → `[RANLS]` (WM-identity only — `settings_storage` path is `executableDir()`-derived, not app-id-derived, so nothing moved; `rapfi-gui.settings` filename left to avoid orphaning user settings). Full build-target rename `rapfi-gui` → `ranls-gui` (+ `-tests`/`-ui-tests`, `RAPFI_GUI_BUILD_TESTS` → `RANLS_GUI_BUILD_TESTS`, `RAPFI_GUI_BIN` → `RANLS_GUI_BIN`) across `CMakeLists.txt` (incl. `DESCRIPTION`), `tests/CMakeLists.txt`, `tests/check_version.cmake`, `build.sh`, `build_msys2.sh`, `README.md` + a stale `test_ui07` comment. Engine binary names and the About dialog untouched; historical/append-only docs left with the old name. +`tests/test_name01_window_title.cpp` (2 cases in `ranls-gui-ui-tests` — real headless `MainWindow`, asserts `get_title() == "RANLS"`). Clean build (only 3 pre-existing warnings); ctest 3/3 (`ranls-gui-tests` 141 cases / 1112 assertions; `ranls-gui-ui-tests` 16 cases / 88 assertions; `rel02-version-single-source` pass); `./build/ranls-gui --version` → `0.1.1` | [detail](fix-log/2026-09-03-name-01-app-wide-rename-ranls.md) |
| 2026-09-04 | ANLZ-01: "Analyze Mode" — a persisted `ViewConfig::analyzeMode` toggle (menu checkbox + a "∞" `Gtk::ToggleButton` in the EngineStatus ▶/■/↻ cluster) that, while on, restarts the engine's analysis on the new current position after every position change so the WinGraph gains a real (measured, never formula-derived) point for every visited ply. New `MainWindow::scheduleAnalyzeModeRestart()` + `analyzeModeScheduled_` — verbatim copy of `maybeStartAutoMove()`'s single-`Glib::signal_idle().connect_once` coalescing, wired to `signal_board_changed` + the engine→Idle transition; idle guards `analyzeMode && isRunning() && engineState()==Idle`, bails on `isEnginesTurn(...)` (lets `maybeStartAutoMove` play), else `stopAnalysis(); analyze()`. `EngineController::analyze()`/`stopAnalysis()` reused unchanged (no second protocol path); `syncAnalyzeModeMenu()` two-way sync mirrors `syncEnginePlaysMenu()`; `onToggleAnalyzeMode()` persists via `persistGameSetup()` (all 4 blocks, STATE-02). Strictly orthogonal to "Engine plays"/ENG-02 (no `revertEnginePlaysToOff()`, no `MatchConfig` write); UI-13 candidate A + its test, one-shot Analyze/Stop, WinGraph maths/UI-01/UI-09/RT-01/save-format all untouched. Q5 text-only `1 − parent` estimate deferred. +`tests/test_anlz01_analyze_mode_coverage.cpp` (3 model cases), +`tests/test_anlz01_analyze_mode_action.cpp` (real `MainWindow` toggle-action round-trip), +1 `test_settings_storage.cpp` case. Build clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. Manual live smoke NOT run (no engine binary / display on the build host) — checklist + reasoning trace in the detail (ANLZ-01) | [detail](fix-log/2026-09-04-analyze-mode.md) |
| 2026-09-04 | ANLZ-04: `WinGraphView::onDraw` bridged interior NaN gaps instead of lifting the pen. New pure helper `computeGapBridges()` (`src/ui/win_graph_bridge.h`) returns one `{fromIdx,toIdx}` pair per interior NaN run (none for leading/trailing / `n<=1` / all-NaN); `onDraw` gained a pass-1 loop per series that strokes those connectors dashed `{4,3}` (distinct from the UI-09 White dash `{6,4}`), series colour @ 0.4 alpha, width `kSeries*W*0.6` — one accumulated path, one `stroke()`, `unset_dash()`, never `set_dash` mid-path — then the pre-existing solid-run loop (pass 2) unchanged. Current-move-dot guard + hover `(no eval)` untouched; nothing written back to the data, no synthesised 50%. `buildWinGraphSeries` / `evalHistory` sentinel / eval maths / UI-09 / RT-01 / axes untouched; no flag/setting/gap-cap. Rendering refinement of UI-01 → rationale in `docs/audit/2026-09-04-wingraph-nan-bridge.md`. +`tests/test_anlz04_wingraph_bridge.cpp` (8 cases / 21 assertions in `ranls-gui-tests`). Build clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. Manual live/visual smoke not possible (no display) — substituted by the helper unit test (see detail) | [detail](fix-log/2026-09-04-anlz-04-wingraph-nan-bridge.md) |
| 2026-09-04 | RDB-01: storage substrate for the new `.rdb` binary save format (first of RDB-01/02/03, integration branch `feat/rdb-save-format`). New `src/model/rdb/`: `compressor.{h,cpp}` (`ICompressor` + `RawCodec` id 0 + `DeflateCodec` id 2 over zlib level 7; reserved zstd id 1 unimplemented), `rdb_container.{h,cpp}` (`"RDB1"` magic + explicit little-endian header, always-on zlib crc32, atomic `.tmp`→fflush→fsync→`rename`; `readContainer` validates magic/version/codec/packed-size/crc before allocating — every failure ⇒ empty `std::optional` + error, never throws), `game_graph.h` (stdlib-only serialisation DTO), `game_graph_cbor.{h,cpp}` (hand-rolled total RFC 8949 subset — major types 0–5, bool/null, `0xFB` write + `0xFA` read; bounds-checks before every read; unknown map keys recursively skipped; depth-capped; `winrate` outside `[0,1]` ⇒ absent), `game_graph_convert.{h,cpp}` (only unit including `variation_tree.h`: `toGameGraph` flat DFS pre-order + parent indices + root sentinel, no analysis block for NaN eval; `applyGameGraph` rejects forward/self/out-of-range parent + bad schema/board/rule before mutating, restores only `move`/`eval`/`nodes`/`depth`/`comment`). Build: `find_package(ZLIB REQUIRED)` + `ZLIB::ZLIB` on `ranls-gui` + both test targets; 4 new `.cpp` in `GUI_SOURCES` + both test targets. No UI / `GameIO` / `TreeNode` / zstd change (RDB-02/03). +`tests/test_rdb01_container.cpp` + `tests/test_rdb01_game_graph.cpp` (15 cases / 984 assertions: Raw+Deflate round-trip, Deflate<Raw, all corrupt-container cases ⇒ empty+error, 400+500 random truncations never crash, CBOR round-trip w/ analysed+NaN+branches+unicode, unknown-key skip, winrate 1.4 ⇒ absent, `toGameGraph`∘`applyGameGraph` structure+eval fidelity w/ NaN preserved, bad parent refs rejected). Build clean; ctest 3/3 | [detail](fix-log/2026-09-04-rdb-01-container-and-codec.md) |
| 2026-09-04 | RDB-02: wired the `.rdb` binary save format into Save/Open and retired `.yxgame` write. New `src/model/rdb/game_archive.{h,cpp}` — `IGameArchiveReader`/`IGameArchiveWriter`; `RdbArchive` (RDB-01 `writeContainer`/`readContainer` + DEFLATE + `encodeCbor`/`decodeCbor`, both directions); `YxgameReader` (read-only, wraps the unchanged `GameIO::loadGame` → linear-chain `GameGraph`, no per-node `analysis` ⇒ every eval NaN); `archiveReaderFor`/`archiveWriterFor` factory by lowercased extension (`.rdb`⇒`RdbArchive`, `.yxgame`⇒`YxgameReader` read / no writer, other⇒`RdbArchive` read). `applyGameGraphToState(GameState&, GameGraph, err)` — testable "apply on load" helper: validates board/rule/every parent ref AND every move coord vs `graph.board` (carried RDB-01-review note) before mutating; then `newGame`→`setRule`→rebuild the full tree via `tree().addMove` (branches on the tree directly)→replay the mainline first-child chain via `makeMove`→emit `signal_tree_updated`+`signal_board_changed`. `MainWindow::onSaveGame` (default `game.rdb`, `.rdb` filter, `toGameGraph`→`archiveWriterFor(path)->save`) / `onLoadGame` (`.rdb`+`.yxgame`+all filters, `archiveReaderFor(path)->load`→`applyGameGraphToState`→`sendConfig`, stops Analyze Mode first). `GameIO::saveGame` deleted (decl+def+header comment); `test_io01` save cases rewritten as hand-written-file load cases (all load / corrupt-input cases kept). Build clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. +`tests/test_rdb02_archive.cpp` (5 cases / 62 assertions) +`tests/test_rdb02_save_open.cpp` (2 cases / 14 assertions, display-skip guarded) | [detail](fix-log/2026-09-04-rdb-02-wire-rdb-into-save-open.md) |
| 2026-09-04 | RDB-03: persist + restore per-node analysis end-to-end — closes the original ANLZ-03 goal (save a populated WinGraph, reopen, graph is back, no re-analysis / no engine call). Last of RDB-01/02/03 on `feat/rdb-save-format`. **D1 (evalHistory gate):** took the preferred path — gate `(depth>0 \|\| nodes>0)` → `!std::isnan(node->eval)`, and re-defaulted `TreeNode::eval` `0.0`→`quiet_NaN()` so a fresh unanalysed node stays a NaN gap (not a false 0.0). Only consequential non-scope file: `src/ui/tree_explorer.cpp` eval column NaN-guarded (`0.00`→`-` for unanalysed rows). `setAnalysisData` `!=` write-guards unaffected (NaN compares unequal). UI-01/UI-13 tests green, unchanged — the gate change does not ripple into them. **D2 (TreeNode shape):** added `std::optional<NodeAnalysisExtras>` member (`evalText` / `pv` `vector<Coord>` / `glyph` / `engineRef=-1` / `analyzedUtc=0`); `eval`/`nodes`/`depth` stay put. Named `…Extras` to avoid the `rdb::NodeAnalysis` DTO clash. `toGameGraph` now writes the full analysis (winrate+depth+nodes+evalText+pv+glyph+engineRef+analyzedUtc) for every non-NaN node; NaN node ⇒ no block. New shared `applyNodeAnalysis()` helper used by both `applyGameGraph` + `applyGameGraphToState`: winrate absent/∉[0,1] ⇒ drop block + eval NaN (never abort); each pv coord validated vs board ⇒ whole pv dropped on any off-board coord. `GraphMeta` gains `engines`; `onSaveGame` fills one display-only `EngineInfo` from `EngineConfig`; empty engines list never fails a load. Load-path cache invalidation already covered via `newGame()` inside `applyGameGraphToState`. **`kSchemaVersion` NOT bumped** (stays 1) — the pv/g/e/ts keys were already in schema 1's CBOR codec + DTO; RDB-03 only populates more of the existing key set, no key added/renamed. Build clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3. +`tests/test_rdb03_node_analysis.cpp` (8 cases, `ranls-gui-tests`) +`tests/test_rdb03_ui_node_analysis.cpp` (1 case, `ranls-gui-ui-tests`, display-skip guarded). Manual live/visual WinGraph smoke NOT run (no display/engine on build host) — noted outstanding in the detail | [detail](fix-log/2026-09-04-rdb-03-persist-node-analysis.md) |
| 2026-09-04 | ANLZ-05: Analyze Mode never auto-moves + a board click mid-search places the stone. `MainWindow`-orchestration only — `maybeStartAutoMove()`'s idle body bails on `gameState_.viewConfig().analyzeMode` (engine never auto-plays while Analyze Mode is on, not even its own assigned turn — reverses planning.md Q6 for Analyze Mode; with it off, auto-move / ENG-02 unchanged); `scheduleAnalyzeModeRestart()` dropped its `isEnginesTurn(...)` early-return so the engine's-turn position is analysed too; the `signal_move_clicked` handler calls `controller_.stopAnalysis()` (synchronous `analyzing_` clear + to Idle) before `gameState_.makeMove(pos)` when `controller_.isAnalyzing()`, gated so a click with no engine/search stays a plain `makeMove()`. `GameState::makeMove()`'s `if (analyzing_) return false;` guard deliberately unchanged; no `revertEnginePlaysToOff()` / `MatchConfig` write (ENG-02 orthogonality); no `ViewConfig` field / Settings row / protocol change. `main_window.h` gained a test-only `friend struct RanlsAnlz05Probe`. planning.md carries a dated implementation note under the existing Q6-reversal section. +`tests/test_anlz05_stop_then_move.cpp` (2 cases, `ranls-gui-tests`), +`tests/test_anlz05_no_automove_action.cpp` (1 case, `ranls-gui-ui-tests`, real `MainWindow`, wire-line spy). No existing ANLZ-01 test asserted the old Q6 skip, so none updated. Build clean (only the 3 pre-existing `-Wunused-function` warnings); ctest 3/3 (`ranls-gui-tests` 183 cases / 2317 assertions; `ranls-gui-ui-tests` 21 / 140). Manual live-engine smoke NOT run (no engine/display on build host) — checklist in the instruction file | [detail](fix-log/2026-09-04-anlz05-analyze-mode-no-automove-mid-search-click.md) |
| 2026-09-04 | ANLZ-06: an Analyze-Mode search's best move was auto-played on the board — closes a gap ANLZ-05 asserted but never enforced. `EngineController::connectProtocolSignals()`'s `protocol_->signal_move` handler relayed every inbound engine coordinate line to `signal_engine_move` unconditionally, and `analyze()`'s `YXNBEST` search always ends by emitting its best move as a bare coordinate line (not a non-committal "analyse only" command per the Rapfi protocol). `src/engine/engine_controller.{h,cpp}` only: new private `enum class SearchIntent { None, Analysis, Move }` member; `analyze()`/`requestEngineMove()` set it to `Analysis`/`Move`; the `signal_move` handler now captures+resets it before deciding, runs the UI-13 flush/state bookkeeping regardless of intent, and emits `signal_engine_move` only when `intent == Move`; reset to `None` on every stop path (`stopAnalysis()`, `stopEngine()`, `signal_process_died`) so a trailing coordinate line for an aborted search is inert even after `state_` has already moved on. `generateAnalyzeRequest()`/`YXNBEST` shape, the ANLZ-05 `MainWindow` guards, `GameState::makeMove()`'s `analyzing_` guard, and ENG-02/UI-06/one-shot-Analyze/auto-move-with-Analyze-Mode-off all untouched. +`tests/test_anlz06_search_intent_gate.cpp` (4 cases, `ranls-gui-tests`) — feeds **inbound** coordinate lines via `EngineProcess::signal_line_received.emit(...)` (the gap in the ANLZ-05 outbound-only test): analysis-intent coord discarded + state settles to Idle + analysis flushed; move-intent coord fires `signal_engine_move` once; post-`stopAnalysis()` late coord inert; full stop→user-move→late-engine-coord regression stays at 1 move past the pre-click position (no double move). `test_anlz05_*` re-checked green in isolation. Build clean (same 3 pre-existing `-Wunused-function` warnings, none new); ctest 3/3. Manual live-engine smoke NOT run (no engine/display on build host) — checklist in the instruction file | [detail](fix-log/2026-09-04-analyze-mode-search-plays-stray-move.md) |
| 2026-09-04 | ANLZ-07: once ANLZ-06 correctly discarded an Analyze-Mode search's terminal coordinate, `scheduleAnalyzeModeRestart()` re-armed a brand new `YXBOARD`+`YXNBEST` round-trip on every Idle transition with no check that the position/result had changed — a search that converged quickly (a forced mate, or any small/solved position) busy-looped `STOP`→redump→search→discard forever at native CPU speed. Fix (skip-restart-if-unchanged only, no backstop timer, per user-resolved design): new `EngineController::analysisConverged()` compares the just-completed analysis-intent search's result (best move + eval text, derived from data `signal_analysis` already parsed — no new engine query) to the previous completed result *for the same `currentPath()`*; `MainWindow::scheduleAnalyzeModeRestart()` gained a `force` parameter (default false) that skips re-arming when converged, with `force=true` at the two call sites that always have a genuine reason to restart (`signal_board_changed` — a real position change; `onToggleAnalyzeMode(true)` — user-requested). Convergence cache is populated only for `SearchIntent::Analysis` completions, so `requestEngineMove()`/ENG-02/UI-06 is unaffected (re-verified green in isolation). ANLZ-01's "every visited position gets analysed" guarantee holds (first completion on any position is never treated as converged). +`tests/test_anlz07_analyze_restart_convergence.cpp` (3 cases, `ranls-gui-ui-tests`): identical-results-in-a-row skips the 2nd restart (proven negative over a 200ms pump window); a changed result keeps restarting; a real position change always restarts even matching a different position's cached result (position-keying doesn't leak). Build clean (same 3 pre-existing `-Wunused-function` warnings, none new); ctest: `ranls-gui-tests` 24/24, `rel02-version-single-source` pass, `ranls-gui-ui-tests` 23/24 (1 pre-existing unrelated `UI-12` scroll-timing flake, confirmed failing identically at the pre-fix commit). Manual live-engine smoke NOT run (no engine/display on build host) | [detail](fix-log/2026-09-04-anlz07-analyze-mode-restart-busy-loop.md) |
| 2026-09-05 | `GomocupProtocol::generateDatabaseQuery()` sent `yxquerydatabaseallt`'s position block as bare `y,x` lines, missing the `,color` field every other position-block generator (`generateAnalyzeRequest`/`generateMoveRequest`) includes. Filed as this bug's root cause; **fully reverted same day (PROTO-04)** — a second transcript proved the bare `y,x` shape works cleanly across 12 back-to-back calls; the format was never the problem (PROTO-03) | [detail](fix-log/2026-09-05-proto-03-database-query-missing-color.md) |
| 2026-09-05 | **Actual root cause of the `ERROR Unknown command` transcript, confirmed against two independent user-provided transcripts**: `stopAnalysis()` flips `EngineController::state_` back to `Idle` synchronously the instant `STOP` is written to stdin, but the real engine subprocess's in-flight YXNBEST search is still asynchronously winding down. The click handler's very next line (`gameState_.makeMove()` → `signal_board_changed` → `queryDatabase()`) fired the next protocol block immediately after, racing the aborted search's still-in-flight teardown on the wire. A second transcript pinned this exactly: 12 back-to-back `yxquerydatabaseallt` calls (identical bare `y,x` format) all clean because the trailing coordinate happened to land before each query; the one failure has the query sent with no coordinate in between. Fix: new `EngineController::sendOrDefer()` gate + `pendingStopFlush_` flag — `stopAnalysis()` arms it only when interrupting a live Analysis-intent (YXNBEST) search (the one case documented to still emit a trailing coordinate after `STOP`); `analyze()`/`requestEngineMove()`/`queryDatabase()`/`sendConfig()` now queue behind it instead of sending immediately, flushed in order once that trailing coordinate confirms the wire is idle; `stopEngine()`/a crashed process both drop the queue outright instead of hanging it forever. Condition-based, no arbitrary timeout, per the user's explicit design ask; `generateDatabaseQuery()`'s format left untouched per the user's explicit instruction. +`tests/test_proto04_stop_flush_race.cpp` (5 cases) (PROTO-04) | [detail](fix-log/2026-09-05-proto-04-stop-flush-race.md) |
| 2026-09-05 | A user-supplied "Gemini Agent" report claimed a SEND protocol burst immediately answered by a RECV burst leaves the Engine Log short of the bottom and disables auto-scroll thereafter. Re-verified from scratch (the report's own diagnosis lived in an out-of-tree scratch harness): confirmed real with an instrumented standalone harness once the Engine Log Notebook tab was actually made the visible page (a hidden page's vadjustment `page_size` stays 0, making the existing UI-10 tests' "at bottom" checks pass vacuously) — GTK4 answers `scroll_to` with a genuine multi-frame kinetic scroll animation of the vadjustment (~150ms), but `programmaticScroll_` is cleared on the very next idle tick, so the animation's own intermediate frames get misread by `sticky_scroll::updateStickOnSettle()` as the user scrolling away, latching `stickToBottom_` off before the view reaches the real bottom. Fix: `scrollEngineLogToBottom()` now also directly `set_value()`s the vadjustment to `upper - page_size` (both the immediate call and the deferred-idle re-issue), skipping the animated transition entirely instead of racing it. No change to `sticky_scroll.h`, `programmaticScroll_`'s scope, the persistent-mark mechanism, or the Move Log path. +`tests/test_ui10_engine_log_scroll_target.cpp` (UI-14 case, fails 5/5 pre-fix, passes 5/5 post-fix). Full suite: `ranls-gui-tests` 194/194; `ranls-gui-ui-tests` 23/25 (2 pre-existing unrelated failures — UI-12 scroll flake + ANLZ-05 engine-wire race — reproduced identically with the fix reverted, confirmed not a regression). Small user-reported bug, no TODO (UI-14) | [detail](fix-log/2026-09-05-ui14-engine-log-burst-scroll-animation-race.md) |
| 2026-09-05 | ENG-03: WM close button ("X") and a GUI crash/`kill -9` both left the engine subprocess running — `signal_close_request` was never wired (only menu-Quit/hotkey stopped the engine), the heap `MainWindow` is never `delete`d so no destructor ever ran, and there was no `PR_SET_PDEATHSIG` so the kernel never killed/reaped an orphaned child either; termination relied entirely on the engine noticing stdin EOF. Fix: `onQuit()`'s body factored into `MainWindow::requestGracefulClose()`, now also called from a new `signal_close_request` handler (vetoes the first close, `controller_.stopEngine()`, `close()` again from the completion callback, `closeInFlight_` guards re-entrancy — relies on `EngineController::stopEngine()`'s existing Stopping-state completion chaining, no second timeout added); `EngineProcess::start()` refactored to `Gio::SubprocessLauncher` + `g_subprocess_launcher_set_child_setup()` arming `PR_SET_PDEATHSIG(SIGKILL)` on Linux (`#ifdef __linux__`, unchanged `Gio::Subprocess::create` fallback elsewhere). `~EngineController`/`~EngineProcess`'s synchronous force-kill path unchanged; ENG-01 enum/`stop()`/`stopAsync()` split untouched. +`tests/test_eng03_close_request.cpp` (widget-level, real `MainWindow`, drives the handler via `g_signal_emit_by_name`, asserts real `Stopping`→`NotStarted` transitions + `MatchConfig::enginePlays` untouched — ENG-02 non-regression) +`tests/test_eng03_pdeathsig.cpp` (forks a harness spawning a stdin-ignoring long-lived script via a real `EngineProcess`, `SIGKILL`s the harness, asserts the child is gone/zombie within ~1.5s — cross-checked by temporarily disabling `prctl()` and confirming the test then fails). Build clean; `ranls-gui-tests` 195/195 assertions; `rel02-version-single-source` pass; `ranls-gui-ui-tests` 25/26 (the 1 failure, `test_anlz05_no_automove_action.cpp`, reproduces identically on unmodified `main` — pre-existing unrelated flake, not a regression). Manual live-engine/display smoke NOT run (no engine binary/display on this build host). Known gap: no Windows/macOS PDEATHSIG equivalent (documented, out of scope) | [detail](fix-log/2026-09-05-eng03-orphaned-engine-on-crash-or-wm-close.md) |
