# Audit log

Index of reviews and non-bug decisions: architecture choices, security reviews, protocol-
compatibility checks against Rapfi/Yixin-protocol changes, build/toolchain decisions. Not a bug
(→ `docs/fix-log.md`) and not a feature (→ `features/<slug>/`).

Append-only, same shape as the fix log: one new `docs/audit/<date>-<slug>.md` detail file, one new
row here. Never edit/reorder/delete an existing row — a wrong past entry gets a new correcting entry.

| Date | Summary | Detail |
|---|---|---|
| 2026-08-21 | Test framework choice for TEST-01: doctest (vendored), no gtkmm-transitive-dependency issue found | [docs/audit/2026-08-21-test-framework-choice.md](audit/2026-08-21-test-framework-choice.md) |
| 2026-08-21 | UX-03: BoardView/TreeNodeView/WinGraphView have no keyboard focus mechanism to draw an indicator for, and no accessible role — accepted as a limitation pending a separate keyboard-navigation feature, not fixed in this pass | [docs/audit/2026-08-21-custom-drawn-widgets-no-keyboard-focus.md](audit/2026-08-21-custom-drawn-widgets-no-keyboard-focus.md) |
| 2026-08-31 | REL-01: stood up user-facing versioning — root `CHANGELOG.md` ("Keep a Changelog", SemVer 0.x) backfilled as a single `## [0.1.0]` covering Sprints 1–6, a "Cutting a release" checklist in the `github` skill + one-line `CLAUDE.md` pointer, and tag `v0.1.0` pushed. Doc/process only (version-string single-sourcing stays REL-02) | [docs/audit/2026-08-31-changelog-and-release-process.md](audit/2026-08-31-changelog-and-release-process.md) |
| 2026-08-30 | Development model identified as personal Scrumban + V-Model traceability spine; adopted a GitHub strategy (per-CODE squash-merge PRs, thin Issues, prefix-mirrored labels, milestone=sprint, hand-synced Projects board) with local `docs/` staying canonical — new `## GitHub project management` CLAUDE.md section + `github` skill | [docs/audit/2026-08-30-github-project-management.md](audit/2026-08-30-github-project-management.md) |
| 2026-08-31 | Installed `obra/superpowers@systematic-debugging` skill and merged it with the user's localization pipeline + existing project skills into a 7-phase "diagnosis pipeline" in `CLAUDE.md` (evidence → reproduce → localize → trace-to-source → hypothesis/minimal-test → fix+defense-in-depth → verify), with a phase→skill map. Doc-only. | [docs/audit/2026-08-31-systematic-debugging-skill-and-pipeline.md](audit/2026-08-31-systematic-debugging-skill-and-pipeline.md) |
| 2026-08-31 | Sprint 7 (`ENG-02`/`UI-08`/`UI-09`/`REL-02`, + `REL-01`) bypassed the per-CODE PR lifecycle — landed as hand commits pushed straight to `main` (branch protection has no `enforce_admins`; `/implement-task` was code-only). Decision: don't unwind `314d434`; backfilled labels + `Sprint 6`/`Sprint 7` milestones; rewrote `/implement-task` + `github` skill to drive branch→PR→merge→sync so it can't recur | [docs/audit/2026-08-31-sprint-7-bypassed-pr-lifecycle.md](audit/2026-08-31-sprint-7-bypassed-pr-lifecycle.md) |
| 2026-09-03 | Synchronized Antigravity agent harness with Claude Code: root `GEMINI.md` symlink to `CLAUDE.md`, `.agents/rules/tracking-files.md`, 7 project skills symlinked + `/implement-task` skill added, `.agents/hooks.json` Stop hook wired with tracking-sync guard. Zero modifications to `.claude/` or `CLAUDE.md` | [docs/audit/2026-09-03-antigravity-workspace-sync.md](audit/2026-09-03-antigravity-workspace-sync.md) |
| 2026-09-04 | ANLZ-04: UI-01's "break the WinGraph into disjoint segments around any NaN run" rendering rule softened to "span an *interior* NaN run with one visually subordinate dashed bridge (dash `{4,3}`, series colour @ 0.4 alpha, `kSeriesW*0.6`) from the last real point before the gap to the first after it". Still no dot on a bridged ply, hover still `(no eval)`, no synthesised 50% — the bridge is a straight line between two real points, nothing written back to the data; leading/trailing runs not bridged. Reason: ANLZ-01's residual gaps (unsearched plies) were fragmenting the trace. `buildWinGraphSeries` / `evalHistory` sentinel / eval maths / UI-09 / RT-01 / axes untouched; no flag/setting/gap-cap | [docs/audit/2026-09-04-wingraph-nan-bridge.md](audit/2026-09-04-wingraph-nan-bridge.md) |
| 2026-09-04 | RDB-02: on-disk save format changed from text `.yxgame` to binary `.rdb` (RDB-01 container + CBOR). `GameIO::saveGame` deleted — no code path writes `.yxgame`; Save offers only `.rdb` (default `game.rdb`). `.yxgame` is import-only: `GameIO::loadGame` unchanged, wrapped by `rdb::YxgameReader` → linear-chain `GameGraph`, no per-node analysis. No migration tool (open + re-save), no recent-files / auto-save / SGF / RenLib. Rationale: `.yxgame` cannot hold the variation tree / comments / analysis and the user rejected extending the text schema (ANLZ-03 → RDB-01/02/03); `.rdb` carries a payload `schema` version + skips unknown keys so RDB-03 layers on without a break | [docs/audit/2026-09-04-rdb-save-format.md](audit/2026-09-04-rdb-save-format.md) |
| 2026-09-06 | Platform-dependency audit (`/systematic-debugging` + cross-check against a Gemini report). Verified matrix: Linux GCC/Clang PASS; Windows MSYS2/MinGW PASS-degraded; **native MSVC FAIL** (bare `<unistd.h>` + `access(X_OK)` in `settings_dialog.cpp`, GCC-only warning flags, no `WIN32` subsystem); macOS PASS-degraded (settings land in CWD, no PDEATHSIG). Guarded already: `PR_SET_PDEATHSIG` / `/proc/self/exe`. Unguarded gaps: `settings_dialog.cpp` `access(X_OK)` (only real compile blocker), `rdb_container.cpp` `fsync` with no Windows `_commit`, 7 test suites hardcode `/bin/cat`/`/bin/true`. Follow-up (after a Gemini advisory pass) tiered into PORT-01 (Tier-1 guarded fixes → Sprint 14), PORT-02 (GResource style.css → Backlog), PORT-03 (MSVC/CI harness → Backlog, gated). Product decision: keep the portable-app settings model. No code changed | [docs/audit/2026-09-06-platform-dependency-audit.md](audit/2026-09-06-platform-dependency-audit.md) |
| 2026-09-06 | Native MSVC build + Windows CI confirmed a supported project goal (user decision) — un-gates PORT-03 (its `Design:` "needs a decision on whether native MSVC / Windows CI is actually wanted" is resolved). Scope unchanged from the todo detail: `if(MSVC)` flag branch + `WIN32` subsystem, a portable `tests/mock_engine/` target replacing `/bin/cat`·`/bin/true` across 7 suites, a `#elif defined(_WIN32)` Win32 Job Object in `engine_process.cpp`, then a Windows GitHub Actions job. Rationale: PORT-01's `_WIN32`/`__APPLE__` branches are already in-tree but unverifiable on the Linux build host — a Windows CI job is the structural fix; the `mock_engine` target has standalone value. Portable-app settings model + "no first-class macOS UI integration" both unchanged. PORT-03 pulled into Sprint 15 Active 2026-09-06 | [docs/audit/2026-09-06-native-msvc-windows-ci-goal.md](audit/2026-09-06-native-msvc-windows-ci-goal.md) |
| 2026-09-06 | `test_anlz05_no_automove_action` was a **PROTO-04 regression** (`502bd77`), not the "pre-existing environmental flake" recorded in `docs/notes/` and re-confirmed as "identical on baseline" through 6 fix-log entries (ENG-03, UI-14, PROTO-03, PORT-01/02/03). Two process defects: (1) PROTO-04's `pendingStopFlush_` change is engine-controller-wide but its verification ran only `ranls-gui-tests`, never `ranls-gui-ui-tests` where the regression test lives; (2) ENG-03 (branched off a `main` already containing `502bd77`) first labelled it "pre-existing", and every later "reproduces on baseline `<hash>`" check inherited a post-`502bd77` baseline — no one bisected to the last-green run (ANLZ-07). Standard set: engine/protocol changes run BOTH suites pre-merge; "reproduces on baseline" proves only "not this branch", never "pre-existing/won't-fix" — bisect + name the commit. Protocol-level "search terminated" signal considered and deferred (real engines close the loop for every realistic position). No CI automation added (repo has no `ranls-gui-ui-tests` gate yet). Code fix: fix-log `2026-09-06-anlz05-uitest-trailing-coordinate-mock-engine.md` | [docs/audit/2026-09-06-anlz05-uitest-misdiagnosis-and-proto04-verification-gap.md](audit/2026-09-06-anlz05-uitest-misdiagnosis-and-proto04-verification-gap.md) |
